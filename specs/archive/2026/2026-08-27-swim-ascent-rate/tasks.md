# Tasks: Water carries a swimmer at rates content declares

**Spec**: [spec.md](spec.md) (SPEC-030) · **Architecture**: [architecture.md](architecture.md), binding · **Branch**: `feature/PRO-992-swim-ascent-rate` · **Created**: 2026-08-27

44 scenarios, each in exactly one task. Measured, not relayed: `grep -cE "^  - FR-[0-9]+\.[0-9]+-S[0-9]+: " spec.md` prints 44 and `grep -oE "^  - FR-[0-9]+\.[0-9]+-S[0-9]+" spec.md | sort | uniq -d` prints nothing. Per phase: 10 · 16 · 9 · 9 = 44. **Two scenarios sit outside their group's phase and both are deliberate**: FR-1.1-S2 is a physics assertion that pins the `9.0` loader default through `launched`, so it lands with FR-4; FR-7.1-S6 is about what the golden directories are *named*, so it lands with the re-shoot that renames them.

Rigor is `high`: a phase's tests are authored and owned by a test author who has not seen the implementation, and the implementation context never edits a test file. **T02, T06, T10 and T13 are the test author's outright**, as are the two field-list mirrors named in T03. **The non-compiling window opens at T01 and closes at T02, and it is one commit wide** — `BlockDefinition` carries public fields and no `Default`, deliberately, so the new field breaks every literal of it at once, and the two halves cannot land in one commit because `git-workflow.md` forbids mixing test and implementation code. Measured here rather than relayed, with the canonical command — `grep -rn "BlockDefinition {" --include=*.rs crates/ | grep -v "pub struct"` — which prints **23 constructions across 20 files**. **The unfiltered command prints 25 across 22 and both extra lines carry `pub struct`**: the type's own declaration at `crates/mc-core/src/block/definition.rs:40`, and `crates/mc-world/tests/luau_declaration_properties.rs:12`, a doc comment quoting this very command. Of the 23, exactly **one is production** (`crates/mc-world/src/content/luau_declaration/mod.rs:189`), one is a bench, and **21 across 18 files are the test author's** — 19 under `tests/`, plus the two sibling unit tests `crates/mc-sim/src/world/mod_test.rs:135` and `crates/mc-world/src/mesh/neighbours_test.rs:75`. Enumerated by file rather than reached by subtraction, and the two totals agree. While the tree does not compile the gate cannot run, so whoever authors tests in that window runs `cargo clippy --workspace --all-targets --all-features -- -D warnings` directly, at that severity (`testing.md` §2). Adding `Default` to save those edits is refused (R5), as is a defaulted third argument on `stating_a_medium`.

**Four constraints the test author's brief must carry**, because each bites only when someone writes a fixture: (a) **FR-4 and FR-5 belong in `crates/mc-sim/tests/support/medium.rs`, never `chamber.rs`** — that module resolves every fixture through `ResolvedVoxels` on purpose, which is the whole of what makes FR-4.1-S9 reach `declared_by`, while `chamber.rs:161` answers `VoxelMedium::NOTHING` unconditionally and would make S9 green while testing nothing (R3); **`chamber.rs` is not to be changed** — `crates/mc-sim/CLAUDE.md` records that a medium under its air was measured to redden 46 tests across twelve files (R4); (b) FR-6's `1e-3` tolerances are **derived from both directions** — above the measured `f32` accumulation error over 60 additions, below the smallest difference the test must still catch — and never loosened until green (R8, `testing.md` §2); (c) FR-5.1-S3's `−2.5` is an **equality and stays one**: the stated declaration pins `r` at zero and `slowed` is bit-exact identity there, which is what the stated fixture buys — a strengthened assertion invalidated the weaker one's fixture wording once already on this spec, and the two move together (R7); (d) a fixture states `solid = false` alongside every medium property unless the scenario says otherwise, because a solid fixture is never overlapped and a test over one measures collision and reports a clean pass.

## Phase 1: A declaration states a swim ascent

- [x] **T01** *(landed `6b94ad7`; box ticked in phase 4, not by phase 1)* `BlockDefinition` gains `swim_ascent: f32`, appended last, with a doc comment stating the one question it answers and what its absence means; the loader's own literal and the bench fixture take the default by hand. **Nothing reads the field yet** — this is the field's existence and no behaviour, which is what makes the FR-1 and FR-2 assertions red for the right reason rather than uncompilable. **The window opens here.** — `crates/mc-core/src/block/definition.rs:40`, `crates/mc-world/src/content/luau_declaration/mod.rs:189`, `crates/mc-world/benches/support/fixtures.rs`
      Scenarios: —
- [x] **T02** *(landed `b8279a3`; box ticked in phase 4, not by phase 1)* **TEST AUTHOR, and the window closes here.** The other 21 constructions across 18 files; `Declaration` (`crates/mc-sim/tests/support/volume.rs:117`) gains the field; `stating_a_medium(swimmable, move_resistance)` (:157) becomes **three-arg rather than gaining a default** — its own doc forbids a builder that states one medium field and leaves another standing, which is the same hazard one field wider — and its ~13 call sites in `crates/mc-sim/tests/support/medium.rs:145-203` move with it, each stating its ascent in full beside the two answers already there. **And one repair in a file this phase otherwise never opens**: `crates/mc-world/tests/luau_declaration_properties.rs:13-14` states *"22 constructions, 21 of them in fixtures, none of which states any of the three"* — a hand-maintained tally over a **growing** population, already stale at 23 before this spec adds a field. **Restate it as the property it is arguing rather than as a count**: the tally does no work in that paragraph, whose argument rests entirely on the universal *"none of which states any of the three"*, and a number is the half that rots while the universal is the half that binds. Keep the command beside it so the claim stays re-checkable. Dating the tally to a tree would leave it true and useless; a derived assertion would be the stronger instrument but owes a positive control (`testing.md` §2) over three fields this spec does not touch.
      Scenarios: — · Depends on: T01
- [x] **T03** *(landed `e26a6e2`; box ticked in phase 4, not by phase 1)* `SWIM_ASCENT_FIELD = "swim_ascent"`; `RECOGNISED_FIELDS` 11 → 12, **appended last after `move_resistance`, in documentation order**, since the refusal quotes the list back line for line; `declared_ascent` is one call to `number::optional_number_at_least_zero` with `SWIM_ASCENT_BY_DEFAULT: f32 = 9.0` — **no new validation vocabulary**, the inherited refusals satisfy FR-2 whole, including the `-0.0 → 0.0` normalisation the fold depends on. The default is a stated constant and **not** a `defaulting_to_solidity`-style derived one. **The two mirrors are test files and the test author's** — `crates/mc-world/tests/luau_declaration_keys.rs:72` and `crates/mc-client/tests/support/quoted_refusals.rs:66` (`FIELDS_IN_THE_ORDER_THE_GUIDE_STATES`, 11 → 12). — `crates/mc-world/src/content/luau_declaration/mod.rs:64-91`, `:141`, `:235`, `.../number.rs:54`
      Scenarios: FR-1.1-S1, FR-1.1-S3, FR-1.1-S4, FR-2.1-S1, FR-2.1-S2, FR-2.1-S3, FR-2.1-S4, FR-2.1-S5, FR-3.1-S1 · Depends on: T02
- [x] **T04** *(landed `667e8f1`; box ticked in phase 4, not by phase 1)* Mod-author documentation: the field with its type, its bound, the value its absence means and the refusal it produces, quoted from a real run; an annotated example that **runs**; the recognised-field list in all four quotations across three pages (`README.md` ×1, `blocks-items.md` ×2, `hot-reload.md` ×1) and the field table, plus `hot-reload.md`'s row and the unguarded prose counts beside them (eleven → twelve). **And the ratio an author can predict from** — `rise/horizontal = (ascent − 0.5)/4.5`, free of the resistance entirely, 1.89 today and 0.667 at the shipped values — which is what an author most needs and cannot derive from a field table. **State that both coefficients are declared per tick at 60 Hz** (Decision 5). — `docs/modding/blocks-items.md`, `docs/modding/README.md`, `docs/modding/hot-reload.md`
      Scenarios: FR-3.1-S2 · Depends on: T03

## Phase 2: The medium carries an ascent to a tick

- [x] **T05** `VoxelMedium` gains `swim_ascent: f32` with `NOTHING` at **`0.0`** and `with` gaining one `.max` line — the same lattice join `resistance` already uses, commutative, associative, idempotent, and with the identity an empty cell needs. Its doc says why `0.0` is the identity while `9.0` is the loader's default, because that disagreement is the whole of Decision 3 and this is the first medium property to have it. — `crates/mc-sim/src/player/mod.rs:128-154`
      Scenarios: FR-4.1-S5, FR-4.1-S8
- [x] **T06** [P] **TEST AUTHOR.** Two repairs to existing tests, both RED-enabling and neither optional. `crates/mc-sim/tests/player_buoyancy.rs:177` gets a **falling start** in the shape the file already uses at `:77` — `PlayerState { velocity: Vec3::new(0.0, -2.0, 0.0), ..adrift() }` — because it starts from `adrift()` at `Vec3::ZERO` and compares two positions a broken `launched` moves together; **do not touch `adrift()`**, which feeds `:136`, `:153` and `:201`, at least two of them about a fall *from rest* by name (R6). Derived so it need not be re-derived: correct keeps `(true, false)`, ascent-only gives `(true, true)`. And `crates/mc-sim/tests/medium_table_width.rs`'s failure message, which claims the index *"fits in one bit"* while the assertion permits two — repaired to state what it enforces and to **name FR-4.1-S10 as the guard holding the one-bit property**; its module prose loses *"both medium questions"* for three. **The `≤ SHIPPED_CEILING` assertion is not tightened** — FR-4.1-S11 asserts one bit by equality beside it.
      Scenarios: —
- [x] **T07** `declared_by` gains the masking arm: **a definition that is not swimmable contributes an ascent of `0.0`**, at `crates/mc-sim/src/replay/medium.rs:119` and **nowhere else**. Not in `with` — that would mint `{false, 0.0, 9.0}` as a distinct entry for every ordinary block, taking the shipped index 1 → 2 bits and +128 KiB at world scale. **Not in the loader** — the placement an implementer reaches for first, because it deletes this arm and looks strictly simpler, and the one no scenario in this phase can see: FR-1.1-S4 and FR-7.1-S2 are what redden it, from phases 1 and 4. Put the masking nowhere at all and **both width tests stay green** while the fountain ships (Decision 4's table). — `crates/mc-sim/src/replay/medium.rs:114-124`
      Scenarios: FR-4.1-S9, FR-4.1-S10, FR-4.1-S11 · Depends on: T05
- [x] **T08** `launched` takes the folded `VoxelMedium` whole — **never the ascent alone**, which is unimplementable: after masking, `{swimmable: true, ascent: 0.0}` and `{swimmable: false, ascent: 0.0}` are the only two things it is ever handed and they must return opposite answers. **The pair first named as the falsifier here — FR-4.1-S3 against FR-4.1-S4 — does not falsify it, measured under mutation in phase 2**: both begin the tick at a vertical velocity of `0.0`, where "carry the velocity forward" and "take this masked zero" are the same number, so neither reddens. The falsifier is **FR-5.1-S3**, the only scenario starting the player already falling, with two pre-existing `player_ground` tests and `player_buoyancy.rs`'s midair jump beside it. The reasoning above stands; the scenario pair does not carry it. Record: `crates/mc-sim/CLAUDE.md`. Its buoyant arm returns `medium.swim_ascent`, its `on_ground` arm still returns `JUMP_SPEED`, its condition is the expression it already was, and `walk_velocity`, `slowed`, `fallen`, `bounded` and `settled` are untouched — **land movement is unchanged and FR-5.1-S2 is the control that pins it**. The module doc gains what replaces `JUMP_SPEED` on the buoyant path, beside the launch order it already states, and the 60 Hz tick-rate binding. — `crates/mc-sim/src/player/physics.rs:111`, `:210-228`
      Scenarios: FR-1.1-S2, FR-4.1-S1, FR-4.1-S2, FR-4.1-S3, FR-4.1-S4, FR-4.1-S6, FR-4.1-S7, FR-5.1-S1, FR-5.1-S2, FR-5.1-S3, FR-5.1-S4 · Depends on: T06, T07
- [x] **T09** [P] The engine reader's standing record, six statements that go false: `docs/technical/architecture.md:569` (`VoxelMedium { swimmable, resistance }`), **`:579` — the table keyed on `(swimmable, move_resistance)` and its 1-bit / 128 KiB derivation, which is the standing statement Decision 3 exists to defend** — and `:550-557`'s player-constants table, whose `Jump speed 9.0` row becomes conditional on the medium; `crates/mc-sim/CLAUDE.md:59-60` and `:69`, the same value and "both halves" where there are now three; `crates/mc-sim/src/replay/medium.rs`'s module doc and `MediumTable::of`'s doc at `:66-70`, both of which key the table on `(swimmable, move_resistance)` by name.
      Scenarios: — · Depends on: T07

## Phase 3: The shipped water, and the golden set re-shoots once

- [x] **T10** **TEST AUTHOR, and it lands before T11.** Two deliverables in `crates/mc-sim/tests/support/`. **A fixture that does not exist**: shipped `base:water` at least four voxels deep and four wide on each horizontal axis, resolved through `content_registry()` (`support/mod.rs:111`) rather than through the fixture registry, with the player's box starting at least one block clear of floor, ceiling and every wall — three of FR-6's sharpest scenarios stand on it, and neither `support/medium.rs` (fixture registry) nor `support/sea.rs` (two voxels deep, less water than a one-second rise crosses) can carry them. **And six archived scenario IDs retargeted** in `support/sea.rs` — `FR-6.1-S6` at `:43` and `:178`, `FR-6.1-S4` at `:61`, `:82`, `:329` and inside the panic string at **`:336`**, which spells out a sink budget *derived from the declared value*, the exact shape FR-6 exists to reverse, inside the fixture FR-6 leans on hardest. A watch length may be derived; a threshold may not. **And one file this task should have named**: `crates/mc-sim/tests/shipped_water_declares_a_medium.rs` holds the `1.6` that `water.luau` and `spec.md` both call the number's only guard, so T11 reddens it with no owner — retargeted in place as FR-6.1-S1's witness rather than given a second witness through the same door, which would re-prove one declared value through one door. **The six line numbers above were measured at `4814afe` and four have since drifted +9/+10** (`:178`→`:187`, `:82`→`:91`, `:329`→`:339`, `:336`→`:346`); they are left as taken rather than renumbered, because a citation re-pointed at a line nobody measured asserts more than it knows — and the retarget has landed, so following them today finding nothing is the success rather than the drift.
      Scenarios: —
- [x] **T11** `move_resistance` 1.6 → 0.5 and `swim_ascent = 3.5` — **and the file's own prose, which this change makes false in two places**: the paragraph deriving 1.6 as *"the geometric centre of the window the six sea scenarios admit"*, and the one stating that *"exactly one test guards this number"* and that a change re-minting the goldens in the same breath is reported by nothing at all. FR-6.1-S2 and S3 are that guard now, absolutely rather than by arithmetic over the value, which is the evidence hole `spec.md` refuses to inherit. **Never tune a declared value until a scenario goes green and never soften a scenario's form to admit one**: a disagreement with the closed forms is a spec finding to escalate. — `content/base/blocks/water.luau`
      Scenarios: FR-6.1-S1, FR-6.1-S2, FR-6.1-S3, FR-6.1-S4, FR-6.1-S5, FR-6.1-S6, FR-6.1-S7, FR-6.1-S8 · Depends on: T10
- [x] **T12** `SCENE_REVISION` `"r3"` → `"r4"` and **all four** golden directories re-shot under the new name — the scripted walk wades the sea, so declared physics is an input to the camera path, and this is the first change to land under SPEC-022's rewritten wording. PRO-904's procedure as written at `docs/technical/rendering.md:1280-1330`: probes, oracle, HUD prediction, mint through `terrain_goldens` and `hud_goldens` **only**, then verify with the opt-in unset including `golden_mismatch`, then `cargo nextest run -p mc-render --test golden_inventory`. **`MYCRAFT_UPDATE_GOLDENS` must never be set for a run selecting `golden_mismatch`** — that is the known corrupting failure mode, and every later run passes forever against the wrong reference. — `crates/mc-render/src/capture.rs:47`, `crates/mc-render/goldens/player-walk{,-hud}-t{000,059,119}-r3`
      Scenarios: FR-7.1-S6 · Depends on: T11
- [x] **T13** **TEST AUTHOR.** Re-derive against the moved poses, never copying a number from a run: `JUDGED_TICKS` (`crates/mc-client/tests/replay_oracle.rs:83`), `SAMPLED_TICKS` (`crates/mc-client/tests/replay_determinism.rs:28`), the declared sample grid in `crates/mc-client/tests/the_sea_the_camera_sees_is_the_water_layer.rs`, and PRO-904's water sample counts — each moved figure recorded in this spec's `test-map.md` with what moved it. A sample straddling the sea's edge is moved for a stated reason, never budgeted for. **And one figure this task missed, the same class as T10's gap**: `HUD_CAPTURE_TICKS`' doc comment (`crates/mc-render/src/capture.rs:66-83`) is pose-dependent, and being production code the test author measured it and correctly declined to touch it. It had carried its `r2` figures through the `r3` bump — **a stale recommendation rather than merely a stale number**, telling a later spec to take tick 119 when tick 0 is least-covered again — and the implementation repaired it as a reversal recording its own history (`1c593c8`), not as a fresh reading.
      Scenarios: — · Depends on: T12
- [x] **T14** [P] The player-facing promise this spec breaks. `docs/user/gameplay.md:82-92` states the old feel in numbers that all become false — re-derive both the sink and the swim fraction. `:85` promises *"reaching the surface from the deepest part of the sea in about a third of a second"*, which under the retune is **0.5 s** to `y = 34.0` and **~1.0 s** to breach at `y = 35.0`; the existing sentence conflates the two, and this picks which of them the player is told.
      Scenarios: — · Depends on: T11

## Phase 4: What a save records, and editing a medium while the game runs

- [x] **T15** `DeclaredBehaviour` gains `swim_ascent`, **appended last**, folded as an `f32` by its bits at the width the physics reads; `BEHAVIOUR_REVISION` 3 → 4; `DeclaredAppearance` and `APPEARANCE_REVISION` are untouched at 3, which is what FR-7.1-S7 catches from the other side — and **FR-7.1-S3 is not that guard**, since it compares two appearance hashes to each other and would stay green if both moved together; `format_test.rs:133` and `save_per_face_appearance.rs:111` are, and they hold `3` by hand. Both structs stay written out by hand and are never derived from `BlockDefinition`. **And the behaviour side owes the mirror of that sentence, which this task did not say and cost a red gate**: moving `BEHAVIOUR_REVISION` obliges *every* file stating a behaviour byte sequence, and there are **two** — `format_test.rs`'s behaviour half and `crates/mc-world/tests/save_declarations.rs`, which pins one fold to a hand-derived hash with the whole byte table in its doc comment. The second is invisible to the obvious instrument: it writes its version as a hand-written `03` byte and says "version 3" in prose, so a needle spelled `REVISION` never finds it. `docs/technical/world-format.md:803-823` had already written that down; nothing routed a reader here to it. — `crates/mc-world/src/persistence/format.rs:309`, `:356`, `:423`
      Scenarios: FR-7.1-S1, FR-7.1-S2, FR-7.1-S3, FR-7.1-S4, FR-7.1-S5, FR-7.1-S7 · Depends on: T01
- [x] **T16** [P] Hot reload needs no new mechanism — `World::adopt` already re-resolves the whole volume and rebuilds the medium table, so the ascent is a fourth answer that rule covers and the masking applies to reloaded content with nothing extra to remember. `changes_geometry`'s **doc-comment** list of fields that change nothing a section shows grows by one while the predicate does not (`reload.rs:94-100`); a reload whose declaration states a refusable `swim_ascent` is refused whole, names the field, and the content already being served goes on being served. — `crates/mc-sim/src/world/reload.rs:72`, `crates/mc-sim/src/world/mod.rs:315`
      Scenarios: FR-8.1-S1, FR-8.1-S2, FR-8.1-S3 · Depends on: T08
- [x] **T17** The behaviour-record narrative, both audiences, and it is one repair with the audiences swapped. Engine reader: `docs/technical/world-format.md:672-780` — the behaviour list at revision 3, the per-move narrative, the "told once per move" prose, and the measured mutation record at `:772`. Player: `docs/user/gameplay.md:352` reads *"three times changed… once for what a block looks like, and twice for what it does"* and *"A save old enough to cross both behaviour changes is told once for each"* — four and three, and *"both"* becomes a count; `:113` likewise promises a report *"once for this change"*.
      Scenarios: — · Depends on: T15

## Notes

Out of scope and recorded, not built: a descend input, the residual drag rate (24.33 s⁻¹ against Minecraft's 4.463), and the control-law asymmetry that stops a swimmer dead the tick input is released. Deferred by `architecture.md`: applying the declared ascent *after* gravity, which removes the rise's tick-rate drift and is declined only because FR-4.1-S2 and FR-4.1-S3 are worded on the current order — the change to reach for if the tick rate ever moves. **Observed in phase 4 and not built**: `.gitattributes` pins `*.png binary` but says nothing about `*.mcw`, so all three committed save fixtures — none of which can ever be re-minted — rely on git's content auto-detection. It works (the revision-3 fixture committed as `Bin 0 -> 65759 bytes`, numstat `- -`), and the two older fixtures are empirical proof across three years of commits; an explicit rule would be belt-and-braces on a shared config file no scenario here names.
