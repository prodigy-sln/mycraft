# Spec Registry

Append-only record of every completed spec — one line each, written by
`/sdd-complete`. Lines persist even when spec folders are deleted or
pruned; the branch name resolves the full content through git history when
needed. This project has no pull requests (`standards/global/git-workflow.md`).

Format: `[folder] · [completed] · [rigor] · [tags] · [summary] · [branch]`

---

- 2026-08-11-frame-capture-harness · 2026-08-11 · high · rendering, testing, golden-frames, wgpu, mc-testkit · Headless frame-capture harness in `mc-testkit`: offscreen wgpu capture, CIELAB ΔE golden comparison and artifact reporting, landing before the renderer it verifies · `feature/PRO-849-frame-capture-harness`
- 2026-08-11-chunk-storage-palette · 2026-08-12 · high · world-format, chunk-storage, palette, block-registry, block-identity, mc-core, mc-world · Palette-compressed 16³ sections in 256-block columns, plus a block registry populated only from data under `content/base/`: a section's storable identity is namespaced names, never runtime ids, so a world survives a change of registration order · `feature/PRO-850-chunk-storage-palette`
- 2026-08-12-greedy-mesher · 2026-08-12 · high · meshing, rendering, determinism, neighbour-culling, benchmarks, mc-world · Binary greedy mesher in `mc-world`: a section's visible faces merged into quads by a fixed scanline sweep and culled against six optional neighbour sections, emitted in a total order that *is* the loop nesting rather than a sort, so identical contents mesh byte-identically whatever the write history, compaction state or registration order — which is what makes a golden frame captured against the mesh reproducible · `feature/PRO-851-greedy-mesher`
- 2026-08-12-terrain-render · 2026-08-12 · high · rendering, wgpu, compute-culling, indirect-draw, goldens, derived-probes, windowed-client, snapshot-seam, ADR-013, mc-render, mc-sim, mc-client · Terrain on a screen: `mc-render`'s pure geometry, packing, culling and window-policy layer behind a compile-enforced `gpu` feature seam, a compute pass compacting indices into one `draw_indexed_indirect`, and a windowed `mc-client` driving a fixed-seed replay that `mc-sim` publishes through `ArcSwap` without either crate naming the other. Verified by committed goldens **and** by derived probes computing every expectation from the declared camera, world and colours — because a golden re-shot from a broken renderer is a golden of a broken renderer. ADR-013 narrows ADR-008's coverage exclusion to `mc-render`'s GPU-resident subtree, which the feature seam is what makes honest. Manual visual acceptance is recorded in `docs/technical/testing.md` as still outstanding · `feature/PRO-852-terrain-render`
- 2026-08-12-player-movement · 2026-08-13 · high · player, physics, collision, camera, input, cursor-capture, intent, authority, ADR-014, mc-sim, mc-client, mc-render · Terrain you can stand on: a free-look camera, WASD under gravity and AABB-vs-voxel collision in `mc-sim`, reached only through a five-field `MovementIntent` that has no way to state a position — so the clamp lives on the receiving side and MVP 3 is a transport swap rather than a rewrite (ADR-014). Physics is a pure `advance_player` over a `Solidity` grid resolved once from a `BlockVolume`, with x-then-z-then-y resolution exact because no declared constant displaces a tick more than 0.8 blocks. Two design-time figures died here against the shipped code: `surface_height(32,32)` is 37 and not the re-implementation's 40, and `advance` takes `&mut self` because a tick assigns `self.player` beside the `ArcSwap` — not because a lost update needed refusing, a race `Send`-but-not-`Sync` makes unreachable. FR-5 is verified as policy and not one scenario as product behaviour; the widened manual acceptance in `docs/technical/testing.md` is what stands in until PRO-873 closes it executably · `feature/PRO-853-player-movement`
- 2026-08-13-client-input-harness · 2026-08-13 · high · input, client-dispatch, testing, mutation-criterion, cursor-capture, ports, seams, residue, mc-client · A hole that was measured, not suspected: 409 tests passed while a client ignoring every keystroke and every mouse motion passed all of them, because the winit `ApplicationHandler` needed a real `Window` nothing constructed and ADR-013 excluded the subtree from coverage. `Session` (`mc-client/src/session.rs`) is the drivable core the dispatch now enters — capture ladder, pointer gate, binding table and the tick's guard-drain-advance — reachable with no event loop, no window and no GPU adapter, with the platform reduced to a `PointerPlatform` port that attempts one grab mode and reports whether it was granted. **No windowed test was possible or would have helped**: `winit::event::KeyEvent` is unconstructible downstream and `winit` synthesizes no key events, which is why the seam sits below the key reduction and why that reduction is the residue's first item. The deliverable is not a test count but a property — five named mutations each turn at least one test red — and three of them have exactly **one** falsifier, so the suite records which scenarios cannot be trimmed. A′ (the drain hoisted out of the simulation guard) kills one test of 41, the scenario a plausible, documented and false coverage claim had already cut; restoring it was the margin between a closed exit criterion and one with zero falsifiers. Manual acceptance narrows from six checks to three — 3, 5 and 6 stay, and their yeses are reset to unobserved because this refactor is exactly the change the procedure says to re-run after. ADR-013 is unamended and its expired rationale recorded as a deferred narrowing · `feature/PRO-873-client-input-harness`
- 2026-08-13-break-place · 2026-08-13 · high · raycast, targeting, block-edit, content-fields, replaceable, remesh, mutation-criterion, authority, ADR-015, mc-sim, mc-world, mc-client, mc-core · A world you can change rather than walk through: a server-side traversal cast from the player's own published eye, break and place behind an `ActionIntent` with no field a position could travel in, and one private `World::write` that resolves solidity once and writes the block store and the collision bitset together or neither. The mirror is kept rather than derived, and ADR-015 records why the attractive option lost — deriving solidity from the store would make a whole defect class unspellable but hand the replay's overlap oracle the very lookup chain it exists to check independently. Content gained three separate fields, `breakable`, `breaks_into` and `replaceable`, and **placement reads `replaceable` and never `solid`**: that is what makes water placeable and what keeps a game rule out of the engine content is supposed to own. Emptying a cell writes a name the world carries rather than one the engine picks, so `no_hardcoded_block_names.rs` still holds exactly one exemption. The deliverable is the mutation table, not the 35 scenarios: 15 mutations, **21 biting spellings, every one measured dead** against the finished suite with 467 tests confirmed executed on each run. Three lessons cost real effort and are recorded in `docs/` rather than re-derived. **A mutation whose falsifier would be a hang is not a falsifier** — A1 was struck as an alias of B1 on that ground, and B1 spelled as a break ran the 10 000-operation rig for 116 s instead of failing, which is why the out-of-reach operation is a *placement*. **Fixture geometry decides whether a scenario can grade anything** — aimed straight down a *latching* press still changes exactly one block, so the click aim is 35.29° below level, and a count of one cannot tell "once" from "at most once". **When two independent checks refuse the same input and `.ok()` discards which one fired, no assertion about the outcome can grade either** — so refusals are asserted by name, and mutation M is graded by a unit test outside the scenario↔mutation mapping, recorded as the weaker guarantee it is rather than netted out. Meets **this spec's** exit criterion — 10 000 places and 10 000 breaks in one run, the expectation folded from the schedule before any `Simulation` exists and pinned at compile time, operations that must be refused asserted by name, and block identities checked across all 65 536 cells — while MVP 1 itself is not finished: PRO-876 (which must precede PRO-855), PRO-855, PRO-856, PRO-869, PRO-874, PRO-875 and PRO-882 all remain · `feature/PRO-854-break-place`
- 2026-08-13-empty-cells · 2026-08-13 · high · world-format, empty-cells, contents-type, palette, section-data, persistence-shape, mesher, goldens, mutation-criterion, mc-world, mc-sim, mc-client · A cell holds a block or nothing, and nothing names nothing: `base:air` is retired from content, from `SHIPPED_NAMES` and from the engine's vocabulary, and emptiness becomes a **palette entry naming nothing** rather than a per-cell `Option` — which would have been a parallel 4096-bit mask, +512 bytes per section, a second source of truth and a branch in the mesher's per-cell path. Carried by `Contents<N>`, a named two-variant type and never a bare `Option`, because every `Option` on this read path already means something else: the palette's means "this position exists", the world's means "this position is inside the world", and nesting emptiness inside one is how a corrupt section or a cell past the edge gets read as ordinary empty space. **`Section::block_at` is where the split is made** — out of bounds is `Err(OutOfBounds { axis })`, empty is `Ok(Contents::Empty)` — and every site keeping both wrappers reads three arms and never two. `SectionData.palette` becomes `Vec<Contents>`, one list and one truth, and that is the on-disk shape PRO-855 inherits without a migration; it is done now precisely so there is nothing to migrate. Construction goes infallible and registry-free the whole way up (`Section`/`ChunkColumn`/`VoxelWorld::empty`), because building an empty world must not fail for a reason that has nothing to do with emptiness. **The golden frames were argued immovable on three total arguments and then measured**: no golden byte changed, and the claim rests on the goldens being shown live in the same tree — red under five implementation mutations, and in validation under a sky refilled with solid stone (174 409, 544 121 and 566 448 of 921 600 pixels past tolerance). An empty section is byte-identical in storage to a one-block one — palette position 0, index 0, width `W0`, zero index bytes — which is the ground that argument stands on. Three lessons cost real effort and are now in `docs/technical/testing.md` rather than waiting to be re-derived. **A falsifier list derived from call paths is a hypothesis**: three were measured wrong the same way — a generator mutation credited with a scenario that never runs the generator, a no-op-write mutation credited with a report field derived from the definition and with a bitset the write path sets regardless, and a row reading "at minimum every scenario that reads a cell" whose sweep had no completion condition. **A mutation spelled against the wrong value delivers half what it claims** — refilling empty space with a non-solid block emits no quad and grades two scenarios instead of four. **A review manifest built from `git diff` omits exactly the riskiest files**: three carried test files were absent from a 61-file manifest that balanced perfectly, and a file absent from a diff is exactly a file whose test may have stopped discriminating — one reviewer returned `GAP` rather than guess, the other returned PASS on all three without opening them, a verdict carrying no evidence and indistinguishable from a real one. Re-decided in a supplementary pass, recorded as marginally weaker than one review over all 61. A gate reported green through `| tail` was in fact red — a pipeline reports its last command's status — which is why the gate's result is never read through a pipe · `feature/PRO-876-empty-cells`
- 2026-08-13-world-persistence · 2026-08-14 · high · persistence, save-format, postcard, atomic-replace, block-identity, definition-hash, untrusted-input, allocation-bounds, supply-chain, ADR-016, mc-world, mc-sim, mc-client · A world that survives quitting: a `VoxelWorld` and the player's place in it written to `saves/world.mcw` and read back whole, with break-and-place edits intact and the player standing where they left off. A save is **one plain file replaced atomically** — sibling staging file, `sync_all`, `fs::rename` — holding a 30-byte hand-read preamble then two successive `postcard` values, the block-name table and the world. The preamble is hand-read for exactly one reason: **the version must be read before anything it governs**, so a build that cannot read a format never reports a complaint about bytes it was never entitled to interpret; version 1 is numbered 1 so an all-zero buffer declares an *unrecognised* version rather than the supported one. The table and world are two top-level values and not one struct, which is what makes "report what this save needs without reading its chunk data" true rather than aspirational. **ADR-016 was derived twice, and the second derivation is the one worth reading.** `bincode` won the first, then `cargo deny` failed on RUSTSEC-2025-0141 — development ceased *permanently* after a harassment incident, "no safe upgrade available" — and it was **removed rather than suppressed**, because no save had shipped and no world existed, so removal was a same-day refactor where suppression would have left an unfixable crate defining every byte the feature writes. The first evaluation ranked candidates on streaming, allocation bounds and `serde` and **had no maintenance criterion at all**; that omission is the whole lesson, and the criterion is now recorded in the ADR. The swap cost **four files, five call sites and zero tests** — the confinement rule (`postcard::` nameable only inside `mc_world::persistence`, every decode error collapsing to one variant at its edge) is what turned a supply-chain failure into a local event, and it is named as the mitigation it turned out to be rather than the style preference it reads as. **The security properties are the expensive knowledge and are shaped by one fact: `postcard` bounds *bytes read*, not memory.** A length prefix drives no allocation ahead of the elements arriving, but a small file still expands — `size_of::<ColumnRecord>()` against a one-byte minimum encoding is 24×, ~48× with `Vec` slack, **measured against crafted input rather than estimated**. The 16 MiB file-length precheck against `File::metadata()` is therefore the *only* thing converting a read bound into a memory bound (~768 MiB peak on a maximally hostile file, 4× the largest legitimate save), so raising it means redoing the amplification arithmetic and never just picking a bigger number. The 256-byte name ceiling is the decoder's scratch buffer and is per-field **only because the DTOs hold owned `String`s**: a `String` reads into the scratch *without advancing* it, a borrowed `&str` decodes through a path that *does*, which would silently turn a per-name ceiling into a cumulative budget with no compiler error and no failing test — **a trap for whoever next adds a field**. The line is drawn at the library: it decodes and is treated as working, we validate everything after, and no test asserts a `postcard` variant or message. **Three outcomes, not two, and the asymmetry is the argument**: a *missing* block is a hard refusal no acceptance can override, because nothing can go in the cell and that is not a judgement a player is positioned to make; a *changed* block (declared behaviour, compared by a stored 64-bit hash) is the player's decision, carried in MVP 1 by `--load-changed-blocks` and by `Acceptance`, an enum with no `Default` so the decision cannot be made by forgetting an argument; an *appearance-only* change loads without asking. Behaviour and appearance are hashed separately because one value for both would make a retextured mod indistinguishable from a rebalanced one, and the only safe response to that ambiguity is prompting on every texture edit — which trains a player to accept without reading. **No unknown-block placeholder is built, and that is decided rather than unfinished: a refusal costs a restart, a placeholder can cost a world** — it is the one option that destroys data, silently, the moment a player saves over a world whose absent blocks were substituted. `SaveNameId` is a `u32` newtype distinct from `PaletteIndex`, carried by *palette entries* and not per voxel — the mis-sizing `world-format.md` named the single highest-risk mistake persistence could make is now discharged rather than warned about. Determinism is structural: the table is a `BTreeMap` written in ascending order and **`HashMap`/`HashSet` appear nowhere in the module**, since per-instance `RandomState` would otherwise make two saves of one world differ by which hash seed a process picked. **The deliverable that outlives the format is a rule the spec produced four times at four layers: a second entry point onto a tested path is untested until something asserts through it.** Four surfaces were green under every test and held by nothing — the refusal ignoring `Acceptance`, `load_world` returning a zeroed player, the launch ignoring `Acceptance`, and `mc_sim::persistence::save` writing both a zeroed player and an empty world — each found by mutation at 140–213 passing tests, each now held by a named falsifier, three of them by exactly one. Coverage reports that the code ran; only a mutation reports whether anything was checking. **And one verification no test can perform is recorded as such: removing `sync_all()` reddens nothing**, because a flush is about surviving the machine stopping and no process that observes its own assertions afterward has stopped — held by review, deliberately, so nobody mistakes coverage for verification. Two mutations that did *not* bite were left as spec gaps rather than defects and filed separately (saving on every ending rather than only a clean close; the load-anyway hint offered even when a block is missing, where accepting cannot help). 88 scenarios, 581 tests, 94.37% line coverage, validation PASS on pass 1 with zero findings of any blocking severity · `feature/PRO-855-world-persistence`
- 2026-08-14-minimal-hud · 2026-08-14 · high · hud, content-declared-ui, screen-space, alpha-blending, srgb-decode, outline-pass, egui, debug-overlay, wall-clock-confinement, supply-chain, licence-exception, derived-prediction, ADR-017, mc-core, mc-world, mc-render, mc-client · The two readouts a sandbox is unusable without, and the instrument for diagnosing the engine behind them — built as the same reasoning applied twice rather than as one feature with an exception. **The crosshair and held-block swatch are content**: three `.toml` files under `content/base/hud/`, loaded through a `HudElementSource` port that names no file and no format, with **zero HUD definitions in Rust** held by a scan whose watch list is *derived from the shipped declarations at test time* — so deleting a content file cannot silently stop the scan watching its name. The engine gained a filled rectangle and a textured rectangle and **does not know what a crosshair is**; the crosshair is composed in content from two bars, because a `kind = "crosshair"` would have put the shape of a crosshair in Rust at identical cost. **The debug overlay is deliberately NOT content, and FR-7 makes that a rule rather than a convention**: a mod that could disable the overlay could disable the instrument pointed at it. That guarantee is narrowed to *loaded, running* content — a declaration that fails to load refuses the launch, and a fault naming file, element and field is a better diagnostic than an overlay — **and the narrowing is sound only because content loads once at startup, so the commit that makes declarations hot-reloadable reopens FR-7**, recorded as a triggered forward constraint rather than a note. **The expensive knowledge is about verification, and four items of it cost real effort.** *Alpha composites in linear space*: `#FFFFFF80` over black is byte **188**, not the 128 the hex digits suggest, and two scenarios drafted at `#808080` **would have gone red against a correct renderer** whose cheapest green is deleting the sRGB decode — the second time this project met this exact edge, the first being the clear colour, and 188 means the *correct* answer in one fixture and a *missing decode* in another, so it is never a verdict on its own. Only a mid-tone grades the decode at all: 0 and 255 are fixed points of the transfer function, so `srgb8_to_linear` could be deleted from the HUD path with all 87 tests green. *A golden's blindness has a precise shape and it is not "small means invisible"* — the ΔE 10 hard ceiling is checked before the area budget, so a crosshair rendered uniformly at `[242,242,242]` returns **Match** while every one of its pixels is the wrong colour, which is why FR-4.1 grades the HUD's 733 pixels per pixel against a prediction derived from the declarations with no area budget, and why `rendering.md`'s "what golden frames cannot see" is now about terrain only. But *a prediction that follows its input is not an oracle for that input*: the predictor derives the ring from the declaration, so deleting `outline` from a shipped file leaves prediction and frame agreeing — closed instead by a content assertion over the **parsed** field, which with the field deleted by hand is the sole failure in 652 tests. *Nothing in the workspace runs `App`*, and `mc-client`/`mc-server` are excluded from coverage **wholesale**, so a workspace figure says nothing about them — one phase predicted wiring the clock adapter into `App` would cover its eight lines, the next wired it and **the coverage triple did not move by a digit**, because coverage is produced by execution and a launched client is not an instrumented run. *Reaches the product* and *reached by a test* are different predicates. Deletion is caught, by the lint (`field overlay_clock is never read`); **wrongness is not** — recording the frame time twice per frame leaves all 191 tests green *and* clippy clean while the client displays double the true frame rate, caught only because a human read four lines and `1000 / 31.25 = 32.0` held. *And a caller search cannot see an unexercised branch inside a function that **is** called* — that is how a nine-line uncovered set was reported as eight, the ninth being a ring eviction needing a sixty-first frame where no scenario drives ten; a non-biting mutation and an uncovered line are the same finding through two instruments sharing no code, which is worth more than either. **The clock is confined, not admitted**: the client's no-wall-clock property is what makes a replay identical at 30 and 300 fps, so `Instant` is reachable in exactly one production file, held by a scan with a positive control and a zero-files-read vacuity guard, and `app.rs` stopped claiming an absolute it no longer satisfies. **`egui` taken without `egui-winit` (ADR-017)**, whose defaults re-arm RUSTSEC-2026-0192 through `wayland-csd-adwaita → sctk-adwaita → ab_glyph → ttf-parser 0.25.1` — the exact crate `winit` was hand-trimmed to remove — and feature unification being additive means our own `default-features = false` cannot undo it, so the unused pin is **disarmed rather than deleted**: deleting it loses the knowledge, leaving it bare arms it for whoever opts in next. `deny.toml` gained the project's **first per-crate licence exception** (`epaint_default_fonts`, two font licences ANDed onto the code half so no branch escapes them), measured before it was written and **not claimed unavoidable** — `default-features = false` drops the crate entirely, 156 → 155 packages, at the price of rasterising nothing. Vendoring lost on **visibility**, not cost: a `.ttf` in this tree is not a crate, so no gate stage would read its licence again, while `cargo deny` re-reads the exception every run and names the reason. PRO-894 owns retiring it and only a public-domain face can. **The largest gap the closed set leaves is filed, not papered over**: nothing can tell a painted readout from a painted rectangle — an adapter painting the literal word `"mutation"` passes all 191 tests — and it cannot be closed by a golden, since the first golden holding rasterised text makes whatever rasterised it the ground truth every machine must reproduce (PRO-897). One launch lesson generalises past Win32: the first F3 reading of **0 of 921 600 pixels** was the *instrument*, not the product — a hand-rolled Win32 `INPUT` wrote the virtual key into padding because the x64 union begins at offset 8 — and what said so was not the zero but the capture reproducing the previous phase's figures exactly. **Before reporting that a product does nothing, confirm the instrument does something, through a channel the suspected failure would not also explain.** The corrected reading: F3 moves 2517 pixels inside x 9..305, y 10..74 with zero outside it, a second press returns to 0 of 921 600, and all three HUD footprints plus the terrain are byte-identical between the frames — which is what makes the overlay pass additive rather than destructive. Three frozen terrain goldens did not move a byte. 98 scenarios, 679 tests, 94,21% lines / 92,2% regions over 5583 tracked, validation PASS on pass 1 with zero findings of any severity — and **no exit criterion is marked met: manual acceptance was not performed, and checks 3, 5 and 6 remain outstanding for a human at the physical machine**, recorded in `docs/technical/testing.md` because the folder stating it no longer exists. Outstanding work filed as PRO-892, PRO-894, PRO-895, PRO-896, PRO-897 and PRO-898 · `feature/PRO-856-minimal-hud`
- 2026-08-14-loaded-save-render · 2026-08-15 · high · rendering, persistence, launch, texture-layers, mesh-entry-point, mc-core, mc-sim, mc-client · A resumed save is drawn from the first frame rather than the first edit: the launch establishes which world it plays before building any, so a resume generates nothing, and the texture key set is the registry's rather than the meshed world's — no expression in the tree derives a layer index from a world. Manual acceptance not performed and no exit criterion marked met · `feature/PRO-900-loaded-save-render`
- 2026-08-14-license-texts · 2026-08-15 · medium · licensing, spdx, provenance, structural-invariant, mc-world · The workspace declared `MIT OR Apache-2.0` and shipped neither text. Both now ship, byte-verified against apache.org and SPDX before placement, behind a check deriving the required set from the declaration rather than a filename list and refusing every construct it does not read — `MIT+` refused, not stripped. No test names the copyright holder, deliberately. Provenance and the Apache appendix trap: `docs/technical/licensing.md` · `feature/PRO-874-license-texts`
- 2026-08-15-voxforge · 2026-08-15 · high · voxel-format, grid-art, preview-render, dda, camera-basis, srgb, seam-verdict, connectivity, cli, tools-root, mutation-criterion, human-sign-off, ADR-D1-D17, voxforge, mc-core · **The feedback loop is the feature; the editing verbs are not.** An agent describes a voxel model as layered ASCII grid art in TOML, renders it from fourteen canonical angles, and corrects itself against the picture — so a mirrored preview does not merely look wrong, it teaches the agent to "fix" correct geometry to match a broken view. First member under `tools/`, which the gate's size stage had never walked: it measured `crates/` alone with `-ErrorAction SilentlyContinue`, so a file under `tools/` would have been **silently unmeasured** while the stage reported "all files within limits" having never opened one. The guard is now **per root**, because a total is vacuous at the granularity that matters — `crates/` contributes ~344 files, so a mistyped root contributes 0, the total is still 344, and the stage passes. Two sibling scans (`no_hardcoded_block_names`, `no_hardcoded_hud_names`) were **green because** they no longer looked where this spec put Rust, and a licence oracle enumerating `crates/` alone was three occurrences of one repo-wide assumption that `the workspace is crates/*`. **The deliverable that outlives the format is the sharpened fixture question**, produced by two measured coverage holes: not "can this fixture tell right from wrong" but **"which code path does this assertion read, and is there any fixture in the file where that path does real work?"** Both holes were an assertion reading a path the mutation never touched while the path it did touch was exercised only where the operation is a no-op. Phase 2: the `y`-descending rule read through fixtures sitting on the vertical mirror's **fixed point** — row 1 of 3 maps onto itself, so inverting the rule outright left all 49 tests green; the fix needed an **even** height (fixed-point-free at every row) *and* `ey ≠ ex` (a transpose stays a grid-*shape* refusal), and taking only the first condition would have traded one defence for the other. Phase 3: the model-wide translation of assembled cells, invisible because `u32` conversion **drops** negative cells rather than misplacing them, so a model losing half its voxels still reported minimum corner `(0,0,0)` — the diagnosis is one line, `left: (…(0,0,0),(1,1,1)), 8` against `right: (…(0,0,0),(4,5,6)), 16`, identical in the field the scenario named. **A live Phase 2 defect was found by a Phase 4 fixture shaped unlike anything before it**: `descending = size.y − 1 − row` computed above the match ran for `y`-sliced parts too, so any part with more depth than height underflowed and silently dropped voxels — `part_tree.rs`'s `[3,2,4]` chain had been losing two of four rows since Phase 3, asserted only through `placed()`. That is the third worked example of a second witness on a differently-shaped fixture earning its keep **without anyone able to name in advance what it would catch**, which is an argument about the limits of enumeration rather than about any mutation. **D4's one camera formula absorbed four new views mid-flight with no special case** — `right = normalize(d × w)`, `up = right × d` — and the four under-corners were added because the ten could not show the underside of any horizontal surface (`bottom` is a plan with no depth cue). For a corner and its under twin `right` is **byte-identical**, so `direction.y`'s sign is the sole discriminator anywhere in the suite; dropping `.normalize()` from one corner arm reddens **exactly one test**, the camera table, and no pixel test at all. **Byte 128 against 115 against 92 is the project's recurring sRGB trap, met a third time**: factors apply in linear space, and the encoded-space mutation returns exactly the 102 the spec names as the wrong answer. **The human sign-off is the only check outside the convention table** and it earned that status: every automated orientation check descends from one table, so the layer mapping and the raycaster could be wrong in a way the other agrees with, and a `right` sign flip on the isometric views *alone* escapes all six automated witnesses. It was **retracted once** — an observation ("that does check out") was recorded as a signature, which it was not, and recording an approval that was not given in a gate artefact is the worse half of that error. **What the sign-off exercise measured is the honest finding: the preview loop is reliable for proportion and orientation and unreliable for structure.** Armrests cantilevered on nothing survived ten views and a contact sheet; what caught them was reading the model definition, and then the reviewer reading a render. `inspect`'s connectivity report was claimed to be the instrument for that class and **is not** — measured as soon as it existed: the defective chair returns one component, no floating voxels, exit 0, because the arm was *connected* by one face and merely unsupported. Connectivity is a graph property, support is a physical one, and 6-connectivity cannot separate a cantilever from a braced joint. An upward-flood overhang observation would, and is filed rather than built. **D15 states plainly that no scenario can distinguish its chosen design from the rejected one** — confirmed by implementing the rejected one and watching all 156 stay green — and a decision admitting it has no grader is worth more than one claiming a grader it does not have. FR-8.3 is the sharpest "policy is not wiring" demonstration here: `main` gutted to ignore the library entirely leaves **123 of 125 green**, caught only by the two tests that start a real process. Validation took three passes rather than two because pass 2's finding was a **scope question rather than a defect** — two assets contradicting a binding Out of Scope clause, built at the user's request and then made the evidence a signed sign-off criterion rests on — escalated rather than resolved by the implementer, decided by the user, confirmed clean by pass 3. The clause now covers sign-off evidence and both files warn that deleting the assets deletes what a signature stands on. 144 scenarios, 25 tasks, 7 phases, 888 tests, 93.76% lines / 91.71% regions, validation PASS with zero findings at every severity · `feature/PRO-905-voxforge`
- 2026-08-15-luau-host-sandbox · 2026-08-16 · high · scripting, luau, mlua, sandbox, call-and-loop-budget, memory-cap, interrupt-latch, quarantine, attachment, cascade-queue, raw-reads, hostile-harness, dependency-closure, crate-root-lint, ADR-022, mc-script, mc-testkit · Invariant 3 stops being a claim and becomes a measurement: a sandboxed, budgeted, memory-capped Luau host whose callback dispatch contains a hostile or careless mod, shipped **before** any `mycraft.*` binding exists to harden afterwards. Nothing in `crates/` depends on `mc-script` at the end of it, and `mc-client`'s resolved closure is asserted to exclude it in every dependency kind, by an enumerated verdict with both vacuity controls. **Four things the design documents said and measurement contradicted, each now the reason a mechanism has the shape it has.** *The interrupt does not count instructions* — it fires at seven opcodes, so a loop body of any size is free, a thousand straight-line statements cost one, a call within script costs two and a call into the host costs one; measured on a 16³ pass, 4,369 ticks bare, 8,465 with a host call per cell, 12,561 with a script call per cell, the last aborting under a budget of 10,000 while the code is the same length in all three. The name was corrected during implementation rather than at consolidation, because "instruction budget" guarantees somebody sizes it against VM instructions and is wrong by the size of every loop body — and the authoring consequence, **cost is reduced by batching calls, never by shortening code**, is the single most useful thing a content author can be told about the limit. *`Lua::set_memory_limit` does not cap allocation* — the allocator's refusal is an ordinary catchable Lua error, and under a 1 MiB limit with nothing else a `pcall`-wrapped bomb caught it, dropped the table, let the collector reclaim it and **looped ten times before returning normally**; what enforces the cap is the interrupt reading `used_memory()` against the entry baseline and **latching**, with `set_memory_limit` set *above* the cap as a backstop against a single allocation jumping the gap between two ticks. Reclaiming afterwards takes two explicit collections, not one. *`sandbox(true)` removes five of the fourteen denied globals and leaves nine standing*, so the host removes all fourteen itself and the **ordering is the rule**: everything is removed or installed before the sandbox closes, because afterwards a write lands in a child table and silently changes nothing — which is how a late-installed host `print` is bypassed by a fall-through to the backend's own writing to raw fd 1, a capability escaping rather than a logging inconvenience. *And a per-crate `[lints.clippy]` table is unbuildable* — cargo hard-errors `cannot override 'workspace.lints' in 'lints'` — so the panic denial is a crate-root `#![deny(...)]` composing with the inherited table, whose scope stops short of `tests/*` where each file is its own crate root and only the gate's `-D warnings` reaches. **The permitted surface is enumerated exactly (31 names, compared both directions), not sampled**, and that is what found `gcinfo`: nobody had named it, and it is denied for *determinism* — a generator branching on heap size returns different terrain for one seed, losing a world rather than leaking a number. A denylist derived by asking what should be removed keeps missing whatever nobody thought to name; four of the eight survivors of the backend's own sandbox were not the four expected. **Freezing a table does not freeze what its metatable points at** — three tables have to be frozen and it is easy to see only two, and the scenario that looks like it covers the hole writes the same verb one table away and stays green through it. **A protected call defeats every limit unless the abort latches**, which is why `while true do pcall(f) end` is contained and why post-abort cleanup in script is impossible; the authoring rule that replaces an on-abort hook is *build then swap, never mutate in place*, and it ships beside the honest answer to "a job bigger than one budget" — a cursor in a closure upvalue — which is the very retention the host cannot bound per attachment. That tension is documented rather than resolved by omitting one half. **Dispatch is never re-entrant**, because synchronous re-entry converts an unbounded cascade into Rust stack growth and a stack overflow is an abort; the mutation that enters inline **took the whole test binary down with it**, observed rather than argued. The queue is bounded in **both** dimensions, since a round bound says nothing about queue length, and refusal is a different fault from deferral because only refusal loses the work. **Quarantine is per `(subject, component)` attachment**, three consecutive faults reset by a success, lifted by releasing the attachment or attaching a new callback — which is what reloading a fixed mod is. **ADR-022 records one VM and why the isolation unit must be a loader's to choose**: `set_memory_limit` is per state (385,952 B each, exactly linear at 1/8/64/256, ~100 µs), so per-mod caps require per-mod states, and per-*component* states would make a content-controlled quantity multiply 389 KiB of unreclaimable overhead spent at registration — invisible to every mechanism that acts during an invocation, an invariant-3 breach manufactured by the isolation mechanism itself. Its residual is retention across invocations, whose damage is **misattribution**, closed by a *derived* trigger — `entry_baseline + memory_cap > memory_backstop`, this invocation could fail for a reason that is not its own — rather than a tuned fraction; faults under it name the host and count toward nothing, and the cost is stated plainly: a looping mod is not quarantined either while it holds, which is loud where misattribution is silent and points at the wrong file. A per-attachment retention ledger was **refused and priced**, not deferred. **The harness is the deliverable**: six named hostile cases, the containment evidence declared per case and exact, and a three-valued verdict — a case whose script fails to compile produces no fault, which is indistinguishable from perfect containment on every axis, so a typo must never retire a hostile shape in silence. It configures nothing, its deny list is generated from the host's own, and a text guard with a fully-committed needle set stops it stating any policy of its own — because every scenario about the harness runs through the harness, so nothing else could see it agreeing with the host by construction. **This crate's tests fail by wedging rather than by asserting**: with the interrupt unarmed the six-case sequence did not return in 42 seconds, so seven tests carry exact-name `terminate-after` overrides and **those names are load-bearing** — a filter matching nothing looks exactly like one never reached. Three mutation tables, 18 mutations; one was absorbed by a second enforcement site and one silently failed to apply, and an unbitten mutation is evidence only once it is confirmed to have landed. Two skeletons were needed per group, because a single deliberately-less host cannot redden both "is quarantined" and "is not quarantined". 56 scenarios, 977 tests, 93.82% lines / 91.8% regions, validation PASS on pass 1 with zero findings from all three reviewers, each payload inspected individually. Deferred: a malformed follow-up entry is passed over silently, so an author whose entry is misspelled gets no signal and their follow-up never runs — never fabricating a target the mod did not store is the deliberate half; the missing diagnostic is the gap · `feature/PRO-916-luau-host-sandbox`
- 2026-08-16-print-refusal-chain · 2026-08-16 · high · reporting, error-chain, diagnostics, refusal-text, non-exhaustive, constructor-doors, source-scan, subprocess-test, documentation-drift-guard, ADR-023, mc-render, mc-client, mc-sim · A mod author who mistypes one field reads which file, which declaration and which field, and fixes it in one edit — where the client used to say `mycraft: the shipped content could not be read` and stop, with "change one file at a time" as the documented way forward. **The chain was being destroyed one layer earlier than the defect report placed it**, and that is the whole reason the repair is not a `source()` walk at the print site: two call sites flattened a typed failure with `.to_string()` to build the reported ending, so a walk added where the text is printed would have walked a `String`. **The renderer is depth-general and three levels is the shortest chain that grades it** — a two-level chain renders identically under a walk that takes one hop and stops, which is exactly the implementation a content refusal (always two layers, because one link is `#[error(transparent)]` and the innermost is a `Display` struct rather than an `Error`) could never distinguish. The whole rendered string is compared rather than searched, because a text that merely *contains* the right words cannot tell a clean rendering from one that appended a separator to an empty layer. **The central property is held by the compiler, not by a scan (ADR-023).** The reported variant is `#[non_exhaustive]` so no crate outside `mc-render` can write its struct literal, and it is reachable only through three constructors that each render the whole chain — so a site that composes report text by hand does not compile. The scan stays for the narrower residual it alone can see: a site interpolating a failure under some other binding name and handing the result to the context parameter, which is a naming convention and not an invariant. **That residual is written down rather than papered over**, and the seam sits in `mc-render` beside `exit_code` for one decisive reason — the client crate is excluded from the coverage denominator wholesale, so reporting written there is reporting nothing measures, which is how the original defect shipped. **A message never states its own cause**, which cost three `Display` strings: two are byte-identical to a player afterwards because the `": "` joiner merely moved out of the format string and into the renderer, and the third — whose message wrapped its cause in a *suffix* an appending renderer cannot express — became guidance supplied separately and appended **after** the whole chain, because a way out says what to do rather than what happened and stranding it ahead of the refusal it answers is what a `Display` suffix does. **The verification lesson is a pairing, and each half is blind to what the other sees.** A source scan grades what may *exist* and cannot see a report that is never *reached*: deleting the `report` call from `main` outright, and separately pointing it at stdout, each leave the scan green, and only a test spawning the built binary and reading its real streams catches either. It is cheap because the missing-root refusal is decided before any device is opened, so no GPU and no display server are involved. The scan's positive control commits **every** needle, because a needle no fixture ever commits is one nobody has watched match anything and a mistyped one reports clean forever — and on its first run the scan reported three sites the planning documents said could not exist. **The documented refusal is guarded against the printed one by recognising the artefact's own shape** — a fenced block whose first line begins `mycraft: ` — rather than a marker an author has to remember, so there is no convention to fall out of step with and a page that stops quoting a refusal changes the verdict to "none found" instead of passing silently; leading whitespace is significant and a dependency bump that restyles the parser's diagnostic reddens it, which is the guard working. **The report is a block and not a line**: `toml` 0.9.12 renders a five-line caret diagnostic, measured by probing the crate rather than inferred, which is also why the field name reaches the author inside the cause rather than in the typed field slot — a reader must not assume that slot is populated. **Two limits of the verification are recorded rather than netted out.** The way-out sentence reaches a player through exactly one production line, inside a redraw needing a device and a display server; replacing its argument with an empty string leaves the entire suite green, and the other construction site is fed only by refusals whose guidance is empty by construction, so mutating it would be a semantically identical program whose green result would look like evidence and be none — it was deliberately not run. What holds the supply is the argument being non-optional and the ending being unconstructable outside its three doors. And four non-fatal notices still write to the process error stream directly, outside the renderer and outside any sink, so nothing can capture, redirect or silence them; the as-built record was corrected to name all four rather than quietly stop being false. 26 scenarios, 1007 tests, 94.03% lines / 92.01% regions, validation PASS on pass 1 with zero findings from all three reviewers, each payload inspected individually · `feature/PRO-939-print-refusal-chain`
