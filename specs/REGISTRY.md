# Spec Registry

Append-only record of every completed spec — one line each, written by
`/sdd-complete`. Lines persist even when spec folders are deleted or
pruned; the branch name resolves the full content through git history when
needed. This project has no pull requests (`standards/global/git-workflow.md`).

Format: `[folder] · [completed] · [rigor] · [tags] · [summary] · [branch]`

---

- 2026-08-11-frame-capture-harness · 2026-08-11 · high · rendering, testing, golden-frames, wgpu, mc-testkit · Headless frame-capture harness in `mc-testkit`: offscreen wgpu capture, CIELAB ΔE golden comparison and artifact reporting, landing before the renderer it verifies · `feature/PRO-849-frame-capture-harness`
- 2026-08-11-chunk-storage-palette · 2026-08-12 · high · world-format, chunk-storage, palette, block-registry, block-identity, mc-core, mc-world · Palette-compressed 16³ sections in 256-block columns, plus a block registry populated only from data under `content/base/`: a section's storable identity is namespaced names, never runtime ids, so a world survives a change of registration order · `feature/PRO-850-chunk-storage-palette`
- 2026-08-12-greedy-mesher · 2026-08-12 · high · meshing, rendering, determinism, neighbour-culling, benchmarks, mc-world · Binary greedy mesher in `mc-world`: a section's visible faces merged into quads by a fixed scanline sweep and culled against six optional neighbour sections, emitted in a total order that *is* the loop nesting rather than a sort, so identical contents mesh byte-identically whatever the write history, compaction state or registration order — which is what makes a golden frame captured against the mesh reproducible · `feature/PRO-851-greedy-mesher`
- 2026-08-12-terrain-render · 2026-08-12 · high · rendering, wgpu, compute-culling, indirect-draw, goldens, derived-probes, windowed-client, snapshot-seam, ADR-013, mc-render, mc-sim, mc-client · Terrain on a screen: `mc-render`'s pure geometry, packing, culling and window-policy layer behind a compile-enforced `gpu` feature seam, a compute pass compacting indices into one `draw_indexed_indirect`, and a windowed `mc-client` driving a fixed-seed replay that `mc-sim` publishes through `ArcSwap` without either crate naming the other. Verified by committed goldens **and** by derived probes computing every expectation from the declared camera, world and colours — because a golden re-shot from a broken renderer is a golden of a broken renderer. ADR-013 narrows ADR-008's coverage exclusion to `mc-render`'s GPU-resident subtree, which the feature seam is what makes honest. Manual visual acceptance is recorded in `docs/technical/testing.md` as still outstanding · `feature/PRO-852-terrain-render`
- 2026-08-12-player-movement · 2026-08-13 · high · player, physics, collision, camera, input, cursor-capture, intent, authority, ADR-014, mc-sim, mc-client, mc-render · Terrain you can stand on: a free-look camera, WASD under gravity and AABB-vs-voxel collision in `mc-sim`, reached only through a five-field `MovementIntent` that has no way to state a position — so the clamp lives on the receiving side and MVP 3 is a transport swap rather than a rewrite (ADR-014). Physics is a pure `advance_player` over a `Solidity` grid resolved once from a `BlockVolume`, with x-then-z-then-y resolution exact because no declared constant displaces a tick more than 0.8 blocks. Two design-time figures died here against the shipped code: `surface_height(32,32)` is 37 and not the re-implementation's 40, and `advance` takes `&mut self` because a tick assigns `self.player` beside the `ArcSwap` — not because a lost update needed refusing, a race `Send`-but-not-`Sync` makes unreachable. FR-5 is verified as policy and not one scenario as product behaviour; the widened manual acceptance in `docs/technical/testing.md` is what stands in until PRO-873 closes it executably · `feature/PRO-853-player-movement`
- 2026-08-13-client-input-harness · 2026-08-13 · high · input, client-dispatch, testing, mutation-criterion, cursor-capture, ports, seams, residue, mc-client · A hole that was measured, not suspected: 409 tests passed while a client ignoring every keystroke and every mouse motion passed all of them, because the winit `ApplicationHandler` needed a real `Window` nothing constructed and ADR-013 excluded the subtree from coverage. `Session` (`mc-client/src/session.rs`) is the drivable core the dispatch now enters — capture ladder, pointer gate, binding table and the tick's guard-drain-advance — reachable with no event loop, no window and no GPU adapter, with the platform reduced to a `PointerPlatform` port that attempts one grab mode and reports whether it was granted. **No windowed test was possible or would have helped**: `winit::event::KeyEvent` is unconstructible downstream and `winit` synthesizes no key events, which is why the seam sits below the key reduction and why that reduction is the residue's first item. The deliverable is not a test count but a property — five named mutations each turn at least one test red — and three of them have exactly **one** falsifier, so the suite records which scenarios cannot be trimmed. A′ (the drain hoisted out of the simulation guard) kills one test of 41, the scenario a plausible, documented and false coverage claim had already cut; restoring it was the margin between a closed exit criterion and one with zero falsifiers. Manual acceptance narrows from six checks to three — 3, 5 and 6 stay, and their yeses are reset to unobserved because this refactor is exactly the change the procedure says to re-run after. ADR-013 is unamended and its expired rationale recorded as a deferred narrowing · `feature/PRO-873-client-input-harness`
- 2026-08-13-break-place · 2026-08-13 · high · raycast, targeting, block-edit, content-fields, replaceable, remesh, mutation-criterion, authority, ADR-015, mc-sim, mc-world, mc-client, mc-core · A world you can change rather than walk through: a server-side traversal cast from the player's own published eye, break and place behind an `ActionIntent` with no field a position could travel in, and one private `World::write` that resolves solidity once and writes the block store and the collision bitset together or neither. The mirror is kept rather than derived, and ADR-015 records why the attractive option lost — deriving solidity from the store would make a whole defect class unspellable but hand the replay's overlap oracle the very lookup chain it exists to check independently. Content gained three separate fields, `breakable`, `breaks_into` and `replaceable`, and **placement reads `replaceable` and never `solid`**: that is what makes water placeable and what keeps a game rule out of the engine content is supposed to own. Emptying a cell writes a name the world carries rather than one the engine picks, so `no_hardcoded_block_names.rs` still holds exactly one exemption. The deliverable is the mutation table, not the 35 scenarios: 15 mutations, **21 biting spellings, every one measured dead** against the finished suite with 467 tests confirmed executed on each run. Three lessons cost real effort and are recorded in `docs/` rather than re-derived. **A mutation whose falsifier would be a hang is not a falsifier** — A1 was struck as an alias of B1 on that ground, and B1 spelled as a break ran the 10 000-operation rig for 116 s instead of failing, which is why the out-of-reach operation is a *placement*. **Fixture geometry decides whether a scenario can grade anything** — aimed straight down a *latching* press still changes exactly one block, so the click aim is 35.29° below level, and a count of one cannot tell "once" from "at most once". **When two independent checks refuse the same input and `.ok()` discards which one fired, no assertion about the outcome can grade either** — so refusals are asserted by name, and mutation M is graded by a unit test outside the scenario↔mutation mapping, recorded as the weaker guarantee it is rather than netted out. Meets **this spec's** exit criterion — 10 000 places and 10 000 breaks in one run, the expectation folded from the schedule before any `Simulation` exists and pinned at compile time, operations that must be refused asserted by name, and block identities checked across all 65 536 cells — while MVP 1 itself is not finished: PRO-876 (which must precede PRO-855), PRO-855, PRO-856, PRO-869, PRO-874, PRO-875 and PRO-882 all remain · `feature/PRO-854-break-place`
- 2026-08-13-empty-cells · 2026-08-13 · high · world-format, empty-cells, contents-type, palette, section-data, persistence-shape, mesher, goldens, mutation-criterion, mc-world, mc-sim, mc-client · A cell holds a block or nothing, and nothing names nothing: `base:air` is retired from content, from `SHIPPED_NAMES` and from the engine's vocabulary, and emptiness becomes a **palette entry naming nothing** rather than a per-cell `Option` — which would have been a parallel 4096-bit mask, +512 bytes per section, a second source of truth and a branch in the mesher's per-cell path. Carried by `Contents<N>`, a named two-variant type and never a bare `Option`, because every `Option` on this read path already means something else: the palette's means "this position exists", the world's means "this position is inside the world", and nesting emptiness inside one is how a corrupt section or a cell past the edge gets read as ordinary empty space. **`Section::block_at` is where the split is made** — out of bounds is `Err(OutOfBounds { axis })`, empty is `Ok(Contents::Empty)` — and every site keeping both wrappers reads three arms and never two. `SectionData.palette` becomes `Vec<Contents>`, one list and one truth, and that is the on-disk shape PRO-855 inherits without a migration; it is done now precisely so there is nothing to migrate. Construction goes infallible and registry-free the whole way up (`Section`/`ChunkColumn`/`VoxelWorld::empty`), because building an empty world must not fail for a reason that has nothing to do with emptiness. **The golden frames were argued immovable on three total arguments and then measured**: no golden byte changed, and the claim rests on the goldens being shown live in the same tree — red under five implementation mutations, and in validation under a sky refilled with solid stone (174 409, 544 121 and 566 448 of 921 600 pixels past tolerance). An empty section is byte-identical in storage to a one-block one — palette position 0, index 0, width `W0`, zero index bytes — which is the ground that argument stands on. Three lessons cost real effort and are now in `docs/technical/testing.md` rather than waiting to be re-derived. **A falsifier list derived from call paths is a hypothesis**: three were measured wrong the same way — a generator mutation credited with a scenario that never runs the generator, a no-op-write mutation credited with a report field derived from the definition and with a bitset the write path sets regardless, and a row reading "at minimum every scenario that reads a cell" whose sweep had no completion condition. **A mutation spelled against the wrong value delivers half what it claims** — refilling empty space with a non-solid block emits no quad and grades two scenarios instead of four. **A review manifest built from `git diff` omits exactly the riskiest files**: three carried test files were absent from a 61-file manifest that balanced perfectly, and a file absent from a diff is exactly a file whose test may have stopped discriminating — one reviewer returned `GAP` rather than guess, the other returned PASS on all three without opening them, a verdict carrying no evidence and indistinguishable from a real one. Re-decided in a supplementary pass, recorded as marginally weaker than one review over all 61. A gate reported green through `| tail` was in fact red — a pipeline reports its last command's status — which is why the gate's result is never read through a pipe · `feature/PRO-876-empty-cells`
- 2026-08-13-world-persistence · 2026-08-14 · high · persistence, save-format, postcard, atomic-replace, block-identity, definition-hash, untrusted-input, allocation-bounds, supply-chain, ADR-016, mc-world, mc-sim, mc-client · A world that survives quitting: a `VoxelWorld` and the player's place in it written to `saves/world.mcw` and read back whole, with break-and-place edits intact and the player standing where they left off. A save is **one plain file replaced atomically** — sibling staging file, `sync_all`, `fs::rename` — holding a 30-byte hand-read preamble then two successive `postcard` values, the block-name table and the world. The preamble is hand-read for exactly one reason: **the version must be read before anything it governs**, so a build that cannot read a format never reports a complaint about bytes it was never entitled to interpret; version 1 is numbered 1 so an all-zero buffer declares an *unrecognised* version rather than the supported one. The table and world are two top-level values and not one struct, which is what makes "report what this save needs without reading its chunk data" true rather than aspirational. **ADR-016 was derived twice, and the second derivation is the one worth reading.** `bincode` won the first, then `cargo deny` failed on RUSTSEC-2025-0141 — development ceased *permanently* after a harassment incident, "no safe upgrade available" — and it was **removed rather than suppressed**, because no save had shipped and no world existed, so removal was a same-day refactor where suppression would have left an unfixable crate defining every byte the feature writes. The first evaluation ranked candidates on streaming, allocation bounds and `serde` and **had no maintenance criterion at all**; that omission is the whole lesson, and the criterion is now recorded in the ADR. The swap cost **four files, five call sites and zero tests** — the confinement rule (`postcard::` nameable only inside `mc_world::persistence`, every decode error collapsing to one variant at its edge) is what turned a supply-chain failure into a local event, and it is named as the mitigation it turned out to be rather than the style preference it reads as. **The security properties are the expensive knowledge and are shaped by one fact: `postcard` bounds *bytes read*, not memory.** A length prefix drives no allocation ahead of the elements arriving, but a small file still expands — `size_of::<ColumnRecord>()` against a one-byte minimum encoding is 24×, ~48× with `Vec` slack, **measured against crafted input rather than estimated**. The 16 MiB file-length precheck against `File::metadata()` is therefore the *only* thing converting a read bound into a memory bound (~768 MiB peak on a maximally hostile file, 4× the largest legitimate save), so raising it means redoing the amplification arithmetic and never just picking a bigger number. The 256-byte name ceiling is the decoder's scratch buffer and is per-field **only because the DTOs hold owned `String`s**: a `String` reads into the scratch *without advancing* it, a borrowed `&str` decodes through a path that *does*, which would silently turn a per-name ceiling into a cumulative budget with no compiler error and no failing test — **a trap for whoever next adds a field**. The line is drawn at the library: it decodes and is treated as working, we validate everything after, and no test asserts a `postcard` variant or message. **Three outcomes, not two, and the asymmetry is the argument**: a *missing* block is a hard refusal no acceptance can override, because nothing can go in the cell and that is not a judgement a player is positioned to make; a *changed* block (declared behaviour, compared by a stored 64-bit hash) is the player's decision, carried in MVP 1 by `--load-changed-blocks` and by `Acceptance`, an enum with no `Default` so the decision cannot be made by forgetting an argument (**superseded by SPEC-020**: loading is the default and the argument, now `--refuse-changed-blocks`, asks for the refusal); an *appearance-only* change loads without asking. Behaviour and appearance are hashed separately because one value for both would make a retextured mod indistinguishable from a rebalanced one, and the only safe response to that ambiguity is reporting every texture edit — which trains a player that a report means nothing. **No unknown-block placeholder is built, and that is decided rather than unfinished: a refusal costs a restart, a placeholder can cost a world** — it is the one option that destroys data, silently, the moment a player saves over a world whose absent blocks were substituted. `SaveNameId` is a `u32` newtype distinct from `PaletteIndex`, carried by *palette entries* and not per voxel — the mis-sizing `world-format.md` named the single highest-risk mistake persistence could make is now discharged rather than warned about. Determinism is structural: the table is a `BTreeMap` written in ascending order and **`HashMap`/`HashSet` appear nowhere in the module**, since per-instance `RandomState` would otherwise make two saves of one world differ by which hash seed a process picked. **The deliverable that outlives the format is a rule the spec produced four times at four layers: a second entry point onto a tested path is untested until something asserts through it.** Four surfaces were green under every test and held by nothing — the refusal ignoring `Acceptance`, `load_world` returning a zeroed player, the launch ignoring `Acceptance`, and `mc_sim::persistence::save` writing both a zeroed player and an empty world — each found by mutation at 140–213 passing tests, each now held by a named falsifier, three of them by exactly one. Coverage reports that the code ran; only a mutation reports whether anything was checking. **And one verification no test can perform is recorded as such: removing `sync_all()` reddens nothing**, because a flush is about surviving the machine stopping and no process that observes its own assertions afterward has stopped — held by review, deliberately, so nobody mistakes coverage for verification. Two mutations that did *not* bite were left as spec gaps rather than defects and filed separately (saving on every ending rather than only a clean close; the load-anyway hint offered even when a block is missing, where accepting cannot help). 88 scenarios, 581 tests, 94.37% line coverage, validation PASS on pass 1 with zero findings of any blocking severity · `feature/PRO-855-world-persistence`
- 2026-08-14-minimal-hud · 2026-08-14 · high · hud, content-declared-ui, screen-space, alpha-blending, srgb-decode, outline-pass, egui, debug-overlay, wall-clock-confinement, supply-chain, licence-exception, derived-prediction, ADR-017, mc-core, mc-world, mc-render, mc-client · The two readouts a sandbox is unusable without, and the instrument for diagnosing the engine behind them — built as the same reasoning applied twice rather than as one feature with an exception. **The crosshair and held-block swatch are content**: three `.toml` files under `content/base/hud/`, loaded through a `HudElementSource` port that names no file and no format, with **zero HUD definitions in Rust** held by a scan whose watch list is *derived from the shipped declarations at test time* — so deleting a content file cannot silently stop the scan watching its name. The engine gained a filled rectangle and a textured rectangle and **does not know what a crosshair is**; the crosshair is composed in content from two bars, because a `kind = "crosshair"` would have put the shape of a crosshair in Rust at identical cost. **The debug overlay is deliberately NOT content, and FR-7 makes that a rule rather than a convention**: a mod that could disable the overlay could disable the instrument pointed at it. That guarantee is narrowed to *loaded, running* content — a declaration that fails to load refuses the launch, and a fault naming file, element and field is a better diagnostic than an overlay — **and the narrowing is sound only because content loads once at startup, so the commit that makes declarations hot-reloadable reopens FR-7**, recorded as a triggered forward constraint rather than a note. **The expensive knowledge is about verification, and four items of it cost real effort.** *Alpha composites in linear space*: `#FFFFFF80` over black is byte **188**, not the 128 the hex digits suggest, and two scenarios drafted at `#808080` **would have gone red against a correct renderer** whose cheapest green is deleting the sRGB decode — the second time this project met this exact edge, the first being the clear colour, and 188 means the *correct* answer in one fixture and a *missing decode* in another, so it is never a verdict on its own. Only a mid-tone grades the decode at all: 0 and 255 are fixed points of the transfer function, so `srgb8_to_linear` could be deleted from the HUD path with all 87 tests green. *A golden's blindness has a precise shape and it is not "small means invisible"* — the ΔE 10 hard ceiling is checked before the area budget, so a crosshair rendered uniformly at `[242,242,242]` returns **Match** while every one of its pixels is the wrong colour, which is why FR-4.1 grades the HUD's 733 pixels per pixel against a prediction derived from the declarations with no area budget, and why `rendering.md`'s "what golden frames cannot see" is now about terrain only. But *a prediction that follows its input is not an oracle for that input*: the predictor derives the ring from the declaration, so deleting `outline` from a shipped file leaves prediction and frame agreeing — closed instead by a content assertion over the **parsed** field, which with the field deleted by hand is the sole failure in 652 tests. *Nothing in the workspace runs `App`*, and `mc-client`/`mc-server` are excluded from coverage **wholesale**, so a workspace figure says nothing about them — one phase predicted wiring the clock adapter into `App` would cover its eight lines, the next wired it and **the coverage triple did not move by a digit**, because coverage is produced by execution and a launched client is not an instrumented run. *Reaches the product* and *reached by a test* are different predicates. Deletion is caught, by the lint (`field overlay_clock is never read`); **wrongness is not** — recording the frame time twice per frame leaves all 191 tests green *and* clippy clean while the client displays double the true frame rate, caught only because a human read four lines and `1000 / 31.25 = 32.0` held. *And a caller search cannot see an unexercised branch inside a function that **is** called* — that is how a nine-line uncovered set was reported as eight, the ninth being a ring eviction needing a sixty-first frame where no scenario drives ten; a non-biting mutation and an uncovered line are the same finding through two instruments sharing no code, which is worth more than either. **The clock is confined, not admitted**: the client's no-wall-clock property is what makes a replay identical at 30 and 300 fps, so `Instant` is reachable in exactly one production file, held by a scan with a positive control and a zero-files-read vacuity guard, and `app.rs` stopped claiming an absolute it no longer satisfies. **`egui` taken without `egui-winit` (ADR-017)**, whose defaults re-arm RUSTSEC-2026-0192 through `wayland-csd-adwaita → sctk-adwaita → ab_glyph → ttf-parser 0.25.1` — the exact crate `winit` was hand-trimmed to remove — and feature unification being additive means our own `default-features = false` cannot undo it, so the unused pin is **disarmed rather than deleted**: deleting it loses the knowledge, leaving it bare arms it for whoever opts in next. `deny.toml` gained the project's **first per-crate licence exception** (`epaint_default_fonts`, two font licences ANDed onto the code half so no branch escapes them), measured before it was written and **not claimed unavoidable** — `default-features = false` drops the crate entirely, 156 → 155 packages, at the price of rasterising nothing. Vendoring lost on **visibility**, not cost: a `.ttf` in this tree is not a crate, so no gate stage would read its licence again, while `cargo deny` re-reads the exception every run and names the reason. PRO-894 owns retiring it and only a public-domain face can. **The largest gap the closed set leaves is filed, not papered over**: nothing can tell a painted readout from a painted rectangle — an adapter painting the literal word `"mutation"` passes all 191 tests — and it cannot be closed by a golden, since the first golden holding rasterised text makes whatever rasterised it the ground truth every machine must reproduce (PRO-897). One launch lesson generalises past Win32: the first F3 reading of **0 of 921 600 pixels** was the *instrument*, not the product — a hand-rolled Win32 `INPUT` wrote the virtual key into padding because the x64 union begins at offset 8 — and what said so was not the zero but the capture reproducing the previous phase's figures exactly. **Before reporting that a product does nothing, confirm the instrument does something, through a channel the suspected failure would not also explain.** The corrected reading: F3 moves 2517 pixels inside x 9..305, y 10..74 with zero outside it, a second press returns to 0 of 921 600, and all three HUD footprints plus the terrain are byte-identical between the frames — which is what makes the overlay pass additive rather than destructive. Three frozen terrain goldens did not move a byte. 98 scenarios, 679 tests, 94,21% lines / 92,2% regions over 5583 tracked, validation PASS on pass 1 with zero findings of any severity — and **no exit criterion is marked met: manual acceptance was not performed, and checks 3, 5 and 6 remain outstanding for a human at the physical machine**, recorded in `docs/technical/testing.md` because the folder stating it no longer exists. Outstanding work filed as PRO-892, PRO-894, PRO-895, PRO-896, PRO-897 and PRO-898 · `feature/PRO-856-minimal-hud`
- 2026-08-14-loaded-save-render · 2026-08-15 · high · rendering, persistence, launch, texture-layers, mesh-entry-point, mc-core, mc-sim, mc-client · A resumed save is drawn from the first frame rather than the first edit: the launch establishes which world it plays before building any, so a resume generates nothing, and the texture key set is the registry's rather than the meshed world's — no expression in the tree derives a layer index from a world. Manual acceptance not performed and no exit criterion marked met · `feature/PRO-900-loaded-save-render`
- 2026-08-14-license-texts · 2026-08-15 · medium · licensing, spdx, provenance, structural-invariant, mc-world · The workspace declared `MIT OR Apache-2.0` and shipped neither text. Both now ship, byte-verified against apache.org and SPDX before placement, behind a check deriving the required set from the declaration rather than a filename list and refusing every construct it does not read — `MIT+` refused, not stripped. No test names the copyright holder, deliberately. Provenance and the Apache appendix trap: `docs/technical/licensing.md` · `feature/PRO-874-license-texts`
- 2026-08-15-voxforge · 2026-08-15 · high · voxel-format, grid-art, preview-render, dda, camera-basis, srgb, seam-verdict, connectivity, cli, tools-root, mutation-criterion, human-sign-off, ADR-D1-D17, voxforge, mc-core · **The feedback loop is the feature; the editing verbs are not.** An agent describes a voxel model as layered ASCII grid art in TOML, renders it from fourteen canonical angles, and corrects itself against the picture — so a mirrored preview does not merely look wrong, it teaches the agent to "fix" correct geometry to match a broken view. First member under `tools/`, which the gate's size stage had never walked: it measured `crates/` alone with `-ErrorAction SilentlyContinue`, so a file under `tools/` would have been **silently unmeasured** while the stage reported "all files within limits" having never opened one. The guard is now **per root**, because a total is vacuous at the granularity that matters — `crates/` contributes ~344 files, so a mistyped root contributes 0, the total is still 344, and the stage passes. Two sibling scans (`no_hardcoded_block_names`, `no_hardcoded_hud_names`) were **green because** they no longer looked where this spec put Rust, and a licence oracle enumerating `crates/` alone was three occurrences of one repo-wide assumption that `the workspace is crates/*`. **The deliverable that outlives the format is the sharpened fixture question**, produced by two measured coverage holes: not "can this fixture tell right from wrong" but **"which code path does this assertion read, and is there any fixture in the file where that path does real work?"** Both holes were an assertion reading a path the mutation never touched while the path it did touch was exercised only where the operation is a no-op. Phase 2: the `y`-descending rule read through fixtures sitting on the vertical mirror's **fixed point** — row 1 of 3 maps onto itself, so inverting the rule outright left all 49 tests green; the fix needed an **even** height (fixed-point-free at every row) *and* `ey ≠ ex` (a transpose stays a grid-*shape* refusal), and taking only the first condition would have traded one defence for the other. Phase 3: the model-wide translation of assembled cells, invisible because `u32` conversion **drops** negative cells rather than misplacing them, so a model losing half its voxels still reported minimum corner `(0,0,0)` — the diagnosis is one line, `left: (…(0,0,0),(1,1,1)), 8` against `right: (…(0,0,0),(4,5,6)), 16`, identical in the field the scenario named. **A live Phase 2 defect was found by a Phase 4 fixture shaped unlike anything before it**: `descending = size.y − 1 − row` computed above the match ran for `y`-sliced parts too, so any part with more depth than height underflowed and silently dropped voxels — `part_tree.rs`'s `[3,2,4]` chain had been losing two of four rows since Phase 3, asserted only through `placed()`. That is the third worked example of a second witness on a differently-shaped fixture earning its keep **without anyone able to name in advance what it would catch**, which is an argument about the limits of enumeration rather than about any mutation. **D4's one camera formula absorbed four new views mid-flight with no special case** — `right = normalize(d × w)`, `up = right × d` — and the four under-corners were added because the ten could not show the underside of any horizontal surface (`bottom` is a plan with no depth cue). For a corner and its under twin `right` is **byte-identical**, so `direction.y`'s sign is the sole discriminator anywhere in the suite; dropping `.normalize()` from one corner arm reddens **exactly one test**, the camera table, and no pixel test at all. **Byte 128 against 115 against 92 is the project's recurring sRGB trap, met a third time**: factors apply in linear space, and the encoded-space mutation returns exactly the 102 the spec names as the wrong answer. **The human sign-off is the only check outside the convention table** and it earned that status: every automated orientation check descends from one table, so the layer mapping and the raycaster could be wrong in a way the other agrees with, and a `right` sign flip on the isometric views *alone* escapes all six automated witnesses. It was **retracted once** — an observation ("that does check out") was recorded as a signature, which it was not, and recording an approval that was not given in a gate artefact is the worse half of that error. **What the sign-off exercise measured is the honest finding: the preview loop is reliable for proportion and orientation and unreliable for structure.** Armrests cantilevered on nothing survived ten views and a contact sheet; what caught them was reading the model definition, and then the reviewer reading a render. `inspect`'s connectivity report was claimed to be the instrument for that class and **is not** — measured as soon as it existed: the defective chair returns one component, no floating voxels, exit 0, because the arm was *connected* by one face and merely unsupported. Connectivity is a graph property, support is a physical one, and 6-connectivity cannot separate a cantilever from a braced joint. An upward-flood overhang observation would, and is filed rather than built. **D15 states plainly that no scenario can distinguish its chosen design from the rejected one** — confirmed by implementing the rejected one and watching all 156 stay green — and a decision admitting it has no grader is worth more than one claiming a grader it does not have. FR-8.3 is the sharpest "policy is not wiring" demonstration here: `main` gutted to ignore the library entirely leaves **123 of 125 green**, caught only by the two tests that start a real process. Validation took three passes rather than two because pass 2's finding was a **scope question rather than a defect** — two assets contradicting a binding Out of Scope clause, built at the user's request and then made the evidence a signed sign-off criterion rests on — escalated rather than resolved by the implementer, decided by the user, confirmed clean by pass 3. The clause now covers sign-off evidence and both files warn that deleting the assets deletes what a signature stands on. 144 scenarios, 25 tasks, 7 phases, 888 tests, 93.76% lines / 91.71% regions, validation PASS with zero findings at every severity · `feature/PRO-905-voxforge`
- 2026-08-15-luau-host-sandbox · 2026-08-16 · high · scripting, luau, mlua, sandbox, call-and-loop-budget, memory-cap, interrupt-latch, quarantine, attachment, cascade-queue, raw-reads, hostile-harness, dependency-closure, crate-root-lint, ADR-022, mc-script, mc-testkit · Invariant 3 stops being a claim and becomes a measurement: a sandboxed, budgeted, memory-capped Luau host whose callback dispatch contains a hostile or careless mod, shipped **before** any `mycraft.*` binding exists to harden afterwards. Nothing in `crates/` depends on `mc-script` at the end of it, and `mc-client`'s resolved closure is asserted to exclude it in every dependency kind, by an enumerated verdict with both vacuity controls. **Four things the design documents said and measurement contradicted, each now the reason a mechanism has the shape it has.** *The interrupt does not count instructions* — it fires at seven opcodes, so a loop body of any size is free, a thousand straight-line statements cost one, a call within script costs two and a call into the host costs one; measured on a 16³ pass, 4,369 ticks bare, 8,465 with a host call per cell, 12,561 with a script call per cell, the last aborting under a budget of 10,000 while the code is the same length in all three. The name was corrected during implementation rather than at consolidation, because "instruction budget" guarantees somebody sizes it against VM instructions and is wrong by the size of every loop body — and the authoring consequence, **cost is reduced by batching calls, never by shortening code**, is the single most useful thing a content author can be told about the limit. *`Lua::set_memory_limit` does not cap allocation* — the allocator's refusal is an ordinary catchable Lua error, and under a 1 MiB limit with nothing else a `pcall`-wrapped bomb caught it, dropped the table, let the collector reclaim it and **looped ten times before returning normally**; what enforces the cap is the interrupt reading `used_memory()` against the entry baseline and **latching**, with `set_memory_limit` set *above* the cap as a backstop against a single allocation jumping the gap between two ticks. Reclaiming afterwards takes two explicit collections, not one. *`sandbox(true)` removes five of the fourteen denied globals and leaves nine standing*, so the host removes all fourteen itself and the **ordering is the rule**: everything is removed or installed before the sandbox closes, because afterwards a write lands in a child table and silently changes nothing — which is how a late-installed host `print` is bypassed by a fall-through to the backend's own writing to raw fd 1, a capability escaping rather than a logging inconvenience. *And a per-crate `[lints.clippy]` table is unbuildable* — cargo hard-errors `cannot override 'workspace.lints' in 'lints'` — so the panic denial is a crate-root `#![deny(...)]` composing with the inherited table, whose scope stops short of `tests/*` where each file is its own crate root and only the gate's `-D warnings` reaches. **The permitted surface is enumerated exactly (31 names, compared both directions), not sampled**, and that is what found `gcinfo`: nobody had named it, and it is denied for *determinism* — a generator branching on heap size returns different terrain for one seed, losing a world rather than leaking a number. A denylist derived by asking what should be removed keeps missing whatever nobody thought to name; four of the eight survivors of the backend's own sandbox were not the four expected. **Freezing a table does not freeze what its metatable points at** — three tables have to be frozen and it is easy to see only two, and the scenario that looks like it covers the hole writes the same verb one table away and stays green through it. **A protected call defeats every limit unless the abort latches**, which is why `while true do pcall(f) end` is contained and why post-abort cleanup in script is impossible; the authoring rule that replaces an on-abort hook is *build then swap, never mutate in place*, and it ships beside the honest answer to "a job bigger than one budget" — a cursor in a closure upvalue — which is the very retention the host cannot bound per attachment. That tension is documented rather than resolved by omitting one half. **Dispatch is never re-entrant**, because synchronous re-entry converts an unbounded cascade into Rust stack growth and a stack overflow is an abort; the mutation that enters inline **took the whole test binary down with it**, observed rather than argued. The queue is bounded in **both** dimensions, since a round bound says nothing about queue length, and refusal is a different fault from deferral because only refusal loses the work. **Quarantine is per `(subject, component)` attachment**, three consecutive faults reset by a success, lifted by releasing the attachment or attaching a new callback — which is what reloading a fixed mod is. **ADR-022 records one VM and why the isolation unit must be a loader's to choose**: `set_memory_limit` is per state (385,952 B each, exactly linear at 1/8/64/256, ~100 µs), so per-mod caps require per-mod states, and per-*component* states would make a content-controlled quantity multiply 389 KiB of unreclaimable overhead spent at registration — invisible to every mechanism that acts during an invocation, an invariant-3 breach manufactured by the isolation mechanism itself. Its residual is retention across invocations, whose damage is **misattribution**, closed by a *derived* trigger — `entry_baseline + memory_cap > memory_backstop`, this invocation could fail for a reason that is not its own — rather than a tuned fraction; faults under it name the host and count toward nothing, and the cost is stated plainly: a looping mod is not quarantined either while it holds, which is loud where misattribution is silent and points at the wrong file. A per-attachment retention ledger was **refused and priced**, not deferred. **The harness is the deliverable**: six named hostile cases, the containment evidence declared per case and exact, and a three-valued verdict — a case whose script fails to compile produces no fault, which is indistinguishable from perfect containment on every axis, so a typo must never retire a hostile shape in silence. It configures nothing, its deny list is generated from the host's own, and a text guard with a fully-committed needle set stops it stating any policy of its own — because every scenario about the harness runs through the harness, so nothing else could see it agreeing with the host by construction. **This crate's tests fail by wedging rather than by asserting**: with the interrupt unarmed the six-case sequence did not return in 42 seconds, so seven tests carry exact-name `terminate-after` overrides and **those names are load-bearing** — a filter matching nothing looks exactly like one never reached. Three mutation tables, 18 mutations; one was absorbed by a second enforcement site and one silently failed to apply, and an unbitten mutation is evidence only once it is confirmed to have landed. Two skeletons were needed per group, because a single deliberately-less host cannot redden both "is quarantined" and "is not quarantined". 56 scenarios, 977 tests, 93.82% lines / 91.8% regions, validation PASS on pass 1 with zero findings from all three reviewers, each payload inspected individually. Deferred: a malformed follow-up entry is passed over silently, so an author whose entry is misspelled gets no signal and their follow-up never runs — never fabricating a target the mod did not store is the deliberate half; the missing diagnostic is the gap · `feature/PRO-916-luau-host-sandbox`
- 2026-08-16-print-refusal-chain · 2026-08-16 · high · reporting, error-chain, diagnostics, refusal-text, non-exhaustive, constructor-doors, source-scan, subprocess-test, documentation-drift-guard, ADR-023, mc-render, mc-client, mc-sim · A mod author who mistypes one field reads which file, which declaration and which field, and fixes it in one edit — where the client used to say `mycraft: the shipped content could not be read` and stop, with "change one file at a time" as the documented way forward. **The chain was being destroyed one layer earlier than the defect report placed it**, and that is the whole reason the repair is not a `source()` walk at the print site: two call sites flattened a typed failure with `.to_string()` to build the reported ending, so a walk added where the text is printed would have walked a `String`. **The renderer is depth-general and three levels is the shortest chain that grades it** — a two-level chain renders identically under a walk that takes one hop and stops, which is exactly the implementation a content refusal (always two layers, because one link is `#[error(transparent)]` and the innermost is a `Display` struct rather than an `Error`) could never distinguish. The whole rendered string is compared rather than searched, because a text that merely *contains* the right words cannot tell a clean rendering from one that appended a separator to an empty layer. **The central property is held by the compiler, not by a scan (ADR-023).** The reported variant is `#[non_exhaustive]` so no crate outside `mc-render` can write its struct literal, and it is reachable only through three constructors that each render the whole chain — so a site that composes report text by hand does not compile. The scan stays for the narrower residual it alone can see: a site interpolating a failure under some other binding name and handing the result to the context parameter, which is a naming convention and not an invariant. **That residual is written down rather than papered over**, and the seam sits in `mc-render` beside `exit_code` for one decisive reason — the client crate is excluded from the coverage denominator wholesale, so reporting written there is reporting nothing measures, which is how the original defect shipped. **A message never states its own cause**, which cost three `Display` strings: two are byte-identical to a player afterwards because the `": "` joiner merely moved out of the format string and into the renderer, and the third — whose message wrapped its cause in a *suffix* an appending renderer cannot express — became guidance supplied separately and appended **after** the whole chain, because a way out says what to do rather than what happened and stranding it ahead of the refusal it answers is what a `Display` suffix does. **The verification lesson is a pairing, and each half is blind to what the other sees.** A source scan grades what may *exist* and cannot see a report that is never *reached*: deleting the `report` call from `main` outright, and separately pointing it at stdout, each leave the scan green, and only a test spawning the built binary and reading its real streams catches either. It is cheap because the missing-root refusal is decided before any device is opened, so no GPU and no display server are involved. The scan's positive control commits **every** needle, because a needle no fixture ever commits is one nobody has watched match anything and a mistyped one reports clean forever — and on its first run the scan reported three sites the planning documents said could not exist. **The documented refusal is guarded against the printed one by recognising the artefact's own shape** — a fenced block whose first line begins `mycraft: ` — rather than a marker an author has to remember, so there is no convention to fall out of step with and a page that stops quoting a refusal changes the verdict to "none found" instead of passing silently; leading whitespace is significant and a dependency bump that restyles the parser's diagnostic reddens it, which is the guard working. **The report is a block and not a line**: `toml` 0.9.12 renders a five-line caret diagnostic, measured by probing the crate rather than inferred, which is also why the field name reaches the author inside the cause rather than in the typed field slot — a reader must not assume that slot is populated. **Two limits of the verification are recorded rather than netted out.** The way-out sentence reaches a player through exactly one production line, inside a redraw needing a device and a display server; replacing its argument with an empty string leaves the entire suite green, and the other construction site is fed only by refusals whose guidance is empty by construction, so mutating it would be a semantically identical program whose green result would look like evidence and be none — it was deliberately not run. What holds the supply is the argument being non-optional and the ending being unconstructable outside its three doors. And four non-fatal notices still write to the process error stream directly, outside the renderer and outside any sink, so nothing can capture, redirect or silence them; the as-built record was corrected to name all four rather than quietly stop being false. 26 scenarios, 1007 tests, 94.03% lines / 92.01% regions, validation PASS on pass 1 with zero findings from all three reviewers, each payload inspected individually · `feature/PRO-939-print-refusal-chain`
- 2026-08-16-blocks-in-luau · 2026-08-17 · high · luau, block-declarations, definition-source, content-seam, resolved-content, layer-assignment, content-root-bounds, raw-key-enumeration, print-truncation, toml-retirement, documentation-guard, mc-core, mc-world, mc-script, mc-sim, mc-client, mc-render · The first thing anyone can author in this engine's own language is a block they can go and stand on: `content/base/blocks/*.luau`, four shipped declarations, and a `docs/modding/` walkthrough that takes somebody who has never read this codebase from an empty file to a block in their hand without a line of Rust. **The registry, the six fields, the defaults and the refusals are unchanged — this swaps where definitions come from and nothing else**, and the exit criterion is decided by that: a world saved against the TOML declarations loads against the Luau ones reporting **no block missing, changed or retextured**, which works only because the persistence layer's two hashes deliberately exclude `origin`. It is the sole scenario comparing a whole resolved definition against an oracle computed before the spec existed, and it **discriminates** — flipping `solid` in one shipped declaration reddens it alone. The golden suites are 10/10 across five binaries run **twice, at the two attributable moments**: after the construction moved and again after the layer assignment changed, because run once at the end a wiring defect and an assignment defect are indistinguishable, and the mismatch detector passing is the control saying the comparison can still fail. **Evaluating a declaration is an entry into script and is guarded like every other one** — the call-and-loop budget, the memory cap, the fourteen denied globals and the frozen environment all apply, which is wiring rather than policy, so FR-4.1's four scenarios are asserted *through the loader*; being all-unwanted they cannot detect a host configured absurdly small, and the one instrument that can is the registering scenario running **at the shipped limits**, measured by replacing the host with a 100,000-tick one and watching it redden alone while all nine guard tests stayed green. **`deny_unknown_fields` was `serde`'s and had to be rebuilt**: the host gained a raw key enumeration, because a host that reads a named field but cannot ask what fields exist can never tell a typo from an absence, and the promise that a misspelling is refused rather than lost is a documented contract. **The metamethod it actually meets is `__iter`, not `__index`** — and measurement moved the target again: on this toolchain `mlua`'s `Table::pairs` is *already* raw against `__iter` and `__pairs`, while `Table::len` **honours `__len`**, so the reachable defect is sizing an enumeration with `len()`, which would report every declaration as carrying no fields at all and lose every typo. That has no scenario and got its own test. Output order is the loader's and never the table's, because Lua leaves hash-part order unspecified and an unstable refusal makes the verbatim documentation guard intermittently red — and one scenario's own wording ("the same order on every run") **cannot fail today**, so the test keeps it and adds the one that can: *which* order, against a fixture where written, state and lexicographic order are three different orders. **Four content-supplied quantities gained a bound and a named refusal**, each accepting its own limit because a bound stated only from the refusing side leaves `>` and `>=` indistinguishable, and their **order is the requirement**: the count refuses before any entry is stat'd, the file size before the host ever sees the text. A fifth, the script output the host retains, **truncates rather than refuses and is the reason it exists at all** — nothing in production had ever called `evaluate`, so an unbounded `Vec<String>` had no route content could reach until this loader built one, and half a million prints inside one budget is tens of gigabytes across a root. **Truncation is reported, and closed by a variant rather than a second accessor**: two accessors make the distinction available, a variant makes it unmissable, and reproducing the available-but-not-taken shape while closing it would have been the sharpest version of the mistake. The free result is the argument for it — three existing readings asserting *nothing* was printed now also reject a record the host had stopped keeping. **Content is the simulation's and the client receives it resolved** (`docs/planning/client-server-split.md`): `ResolvedContent` carries names, texture keys, solidity and a **layer assignment**, and carries none of `replaceable`, `breakable` or `breaks_into` — asserted by discrimination in both directions, because a type with no such field cannot fail a test about not having one. A layer index rides inside every packed vertex, so derivation renumbers every index after an inserted block and textures the world wrong silently; the assignment is now stated where the content was read and honoured where it is drawn, with nothing on the receiving side checking it against a sort, since checking is the same derivation twice and would refuse exactly what it exists to accept. **The sharpest measurement of the spec is that this wiring is not behaviourally falsifiable here**: restoring the production derivation left every behavioural test **and both golden suites green**, because honouring and deriving agree for every root buildable today and permuting the assignment permutes the array texture's fill in the same breath — only the source scan reddens, and it can be retired the day an assignment is appended rather than renumbered. Four chokepoints are watched in the client's own sources rather than type names, because renaming a source does not rename the door; the scan admits **no exemption**, so a local *variable* sharing a needle's spelling fails it, and that false positive is the price of a needle without a carve-out — which is also why the simulation's resolver is named `shipped_directory`, settled as a binding interface decision before any implementation existed. **The retirement surface was larger than a search could measure**: 22 lines across 11 files named the deleted type, exactly right and beside the point, because six further test files reached it through the two preparation entry points naming no type at all — five found by reading and one only by the build. Twenty-six mutations across six phases, **twenty-five bit**, and the one that did not is recorded as structure rather than a gap. Four phases running, a test **no scenario asked for** caught something real. Seven scenarios opened green inside their own phase and every one was mutated before being called a control — flipping a default constant reddens the defaults scenario, which is the measurement separating *green on arrival* from *inert*. A dependency guard was found **passing for a reason that had stopped being true**: naming only `toml`, it could no longer see that block declarations now arrive another way, which is the absence-assertion failure in its sharper form — the thing was not removed, it was replaced by something the guard cannot see. Validation PASS at pass 2 with zero findings at any severity; pass 1 returned two real defects, a declared-text refusal shipping with eighteen stray spaces from a line continuation that never landed — invisible to a test asking only whether both quantities are *mentioned*, and closed by making the page the oracle rather than by adding an assertion — and two MVP-1 TOML claims still standing in `content/CLAUDE.md`. 73 scenarios, 1084 tests, 94.16% lines / 92.29% regions. Deferred: PRO-946 (the guard quotes three of roughly fourteen reachable refusals, and quoting one the run does not print *fails* it), PRO-902/PRO-914 (texture resolution consults no registry, in **two** places, pinned by tests that expire when the gap closes — that red being the success signal), the composition-root move with the dependency-closure guard that cannot pass while one binary hosts both halves, and the host's own two-accessor truncation surface · `feature/PRO-917-blocks-in-luau`
- 2026-08-17-hot-reload · 2026-08-18 · high · hot-reload, content-watcher, settling-window, notify-adapter, root-spelling, layer-append, content-serial, tick-boundary-swap, remesh-marking, batch-supersession, player-clearing, world-extent-eligibility, fixture-form, mutation-criterion, ADR-024, ADR-025, mc-core, mc-world, mc-sim, mc-client, mc-render · **A green suite proved nothing twice, and that is what this spec is for.** A mod author leaves `cargo run -p mc-client` running, edits `content/base/blocks/stone.luau`, saves, and walks through stone with the hole they just dug still there and the tick never stopping — the registry stops being a value held for the run and becomes one published through `ArcSwap` and replaced between ticks, built off the tick thread in a scratch host, all-or-nothing so a refused candidate changes nothing at all. **Both defects it shipped past green tests were the same shape and neither was found by a scenario.** `Session::attach_reload` had **no caller outside test fixtures** — `App::collect_preparation` attached a simulation and a re-mesh worker and no reload, `main::run` moved its content root into the preparation and kept no copy — so 86 of 91 scenarios were green over **five phases of correct, tested machinery** and the shipped client watched nothing; it was found by a **presence guard a test author wrote that no task asked for**, because the absence scan they had been briefed to write would have been satisfied by a product that never reaches the seam. Then, with the wiring landed, hot reload was **still inert**: `mc_sim::content::shipped_directory` is `["content","base"].iter().collect()` — **relative** — and it travelled through `PreparedLaunch::root` unchanged to a relevance rule comparing it against `notify`'s **absolute** reported paths as written, so every player save was classified not-content, no attempt began and no refusal printed either, with **1 188 tests green over a no-op** because every fixture built its root with `tempfile`, which is absolute. **The owner found that one by playing the game.** The generalisation both produced now heads `docs/technical/testing.md`'s findings: *every defect this spec shipped past six phases of green tests was a fixture supplying something in a form no caller uses, or a product path no caller takes — before writing a test, ask what the shipped caller supplies and which shipped path reaches this, and if the answer is "a fixture supplies it" or "no shipped path does", that is the test worth writing, and it is never the one the scenario's wording suggests.* **The repair belongs in the rule, not at the call site, and the vendor was measured rather than reasoned about:** `notify` does **not** canonicalise a watched root — it concatenates the caller's spelling onto the working directory verbatim, so watching `..\..\target\x` reports `E:\_PROJEKTE\MyCraft\crates\mc-world\..\..\target\x\blocks\probe.luau` and `./../../target/x/` reports the dotted, forward-slashed prefix embedded mid-path. That rules out both repairs the obvious way: `current_dir()?.join(root)` accidentally reproduces the un-normalised prefix and goes **green on the plain relative form while the dotted one stays red**, and `fs::canonicalize` yields the `\\?\` verbatim form the vendor never reports. Hence **three spellings of one root** are tested at the adapter itself (absolute as the control that says the fixture works, relative as what the game hands over, dotted-with-trailing-separator as what makes it a rule about paths rather than a patch for one spelling), and the `\\?\` form is deliberately excluded because no caller in this tree produces one. **FR-7.1-S8 was ruled in after phases 1–7 had closed**, and it is stated in the spec rather than folded in silently because a scenario added late is the one a later reader most needs to find: the clearing search's predicate is "not solid", `SolidVoxels::is_solid` answers `false` for every position past the footprint by construction, and the two together put a trapped player **outside the loaded world** — measured, not argued, a player at (2.5, 10, 2.5) moved to (−0.5, 10, −0.5) in a sixteen-block world and already falling one tick later, **with the reload itself standing**, which is why nothing else in the suite noticed. Reachable by walking to an edge and reloading a solidity change. **The siting ruling is the durable half:** the world's extent is passed to `cleared` beside the solidity and `is_solid` is untouched, because **a trait is defined by all of its implementors** and the fixture doubles that answer for a hand-written set of cells have no extent and would have had to invent one; treating outside as *solid* was refused as a lie in a model collision, meshing and physics all read, and one that inverts the moment the world streams. No new vocabulary — the boundary wedge takes `NoClearSpaceWithin { blocks: 8 }`, which FR-7.1-S5 already states and FR-7.1-S6 grades. **The mutation record runs to eight phases and its non-bites are the part worth keeping.** Phase 1: nine named plus the two `World::adopt` guards; two "greens" were **vacuous readings rather than a robust implementation** — both read their headline value from the snapshot standing when the candidate was handed over, and `Simulation::adopt` publishes no tick, so nothing the swap did to the player could change what either assertion held; repaired to read one tick **after** the swap, over a grass floor under a stone ceiling so a correct client cannot legitimately move either player, after which each mutation reddens only its own scenario. Phase 3: five, including the shipped door handing its debouncer `Duration::ZERO`, which leaves every other scenario green and proves both of FR-1.1-S4's instruments are owed. Phase 4: six, five biting. Phase 5: eight, all biting, with the reach mutated to 9 and to 7 so that 8 is bounded from both sides. Phase 8: three — deleting the eligibility check reddens the scenario alone, and **short-circuiting eligibility to `false` reddens the *control* alone**, which is exactly the point of a mandatory paired control: "outside is ineligible" implemented as "nothing is eligible" is invisible to a scenario that asserts a refusal. **Six mutations did not bite and each established something different**, which is why they are recorded rather than netted out. `App::serve` not uploading the reloaded texture layers at all left **234 of 234 green** — an appended layer never reaching the device, a *user-visible* outcome, deleted with nothing to say so — and it became a **production change** rather than a scan: `Unuploaded` in `crates/mc-client/src/upload.rs`, where `stated()` borrows for the boundary assertion and `uploaded_to` consumes and is the only route to the owned `TextureLayers` that `Remesher::retire` needs, so both realistic spellings of the omission fail the build (`E0308`, then `E0425`); the obligation sits at the **report** boundary and not on `retire`, because a witness type only an upload can produce would have broken a device-free suite that drives `retire` legitimately, and giving it a device-free constructor destroys the obligation. A greedy append that still refuses could not be written at all — `appending` returns a new value rather than mutating, so "neither key is appended" is **unspellable rather than unchecked**. Accepting a declaration nested one directory deeper left **403 tests green** across two crates: the rule's own doc states the clause and nothing witnessed it, because the shipped-root oracle walks a root holding no nested declaration, the scratch-file test varies the *extension* and the root-forms test varies the *spelling* — closed at the **port** rather than the client, where both halves are decided on paths that demonstrably arrived, so there is no window and no load sensitivity. Dropping `Extent::contains` while keeping the sign refusal left **1 192 of 1 192 green**, because every out-of-world candidate any fixture reached was *negative* — the same wedge at (13.5, 10, 13.5) puts the cube's far side at 21 against an edge at 16 with nothing negative in it, and under that mutation the far-edge player is thrown to (16.5, 10, 10.5) and falls out too. `World::adopt` assigning `self.registry` **before** resolving solidity survived **1 110 tests**, pre-empted by the admission stage's names-held check — *a backstop with no witness stops backstopping silently*, and it earned two guards mapped to no scenario, in separate functions because "still the registry it had" is satisfied for good by an `adopt` that never assigns one. And FR-4.1-S5's own named mutation cannot bite because solidity is settled before either write, which is the code's shape rather than a gap. **`Remeshing::WorkerGone` ships unwitnessed and knowingly.** `rebuild_batches` ends on exactly two conditions and **both are the `Remesher` itself being dropped**, so no test can hold a live one and ask it about a dead worker; the remaining route is a panic inside the worker, which crate-wide `indexing_slicing` denial and `Result`-returning steps make un-inducible from a fixture. It is defect-reachable and fixture-unreachable — different facts — and it is kept because it converts a silent, permanent, player-visible stop into a reported one; what holds it against silent deletion is `App`'s **exhaustive match with no wildcard**, and what is unheld, and named as a reviewer's, is that the report is *reached* and that its wording is right. Its kin `PreparationError::Layers` gets **no** test and a later phase offering one is refused: that variant guards *nothing*, and the discriminator is what would go wrong silently rather than how hard a branch is to reach. **Three more measurements that decided designs.** The re-mesh marking is **binary** — every section or none — because a selective rule marking the sections whose palettes hold a changed name plus their neighbours marks about **82 of 256** in a world whose highest occupied section is 3 in fifteen columns and 4 in one, and would fail the spec's stated bound outright; the mutation marking 64 of 256 covers every *occupied* section, so **the drawn observable cannot see it** and FR-4.1-S3 passes against a selective rule, which is precisely why the expected value had to be derived from `FOOTPRINT_COLUMNS² × SECTIONS_PER_COLUMN` and never written as a digit — had 82 been written, four scenarios would have agreed with it and the bound would have been silently abandoned. FR-6.2-S4's first draft **called the hand-back itself**, and replacing the frame path's `Superseded` arm with `drop(keys)` left 77 of 77 green; renaming the call changed nothing (a `#[must_use]` `Stale` the test drove itself left 3 of 3 green under the same discard) — the fix was to move the *call*, so `Session::collect_remesh` performs the hand-back, `Session::mark_for_remesh` lost its `pub` and the harness lost the forward, and **a fixture that offers a decision as a call of its own is how a scenario about that decision comes to make it**. And a boundary count is a proxy for elapsed time whose conversion factor **differs by five orders of magnitude** between a test loop (~100 ns) and the shipped client (a rendered frame, ~16 ms) — 21 of 27 phase-3 tests failed on a fixture that gave up an order of magnitude before a 0.7–1.7 ms build could land, *and the same defect ran the other way silently*: 300 tight boundaries span 30 µs, so five runs expecting **no** attempt were right answers for the wrong reason. The later repair is the sharper rule: **no test bound is denominated in `SETTLING_WINDOW` any more**, because that constant is production policy while a fixture bound states how slow this machine may be, and sharing one number made a minimum and a maximum move together so the minimum was short exactly where it had to be long — measured price, 101 s → 126 s for 716 tests. **Latency is measured and not gated**, per FR-9's own reasoning that a flaky wall-clock assertion teaches everybody to re-run it: `crates/mc-sim/benches/reload.rs` reports a candidate build at 0.8 ms and a whole-world 256-section re-mesh at 9.1 ms, 9.9 ms against an 850 ms engine share computed from the two declared constants rather than written as a third. **Validation took two passes.** Pass 1 was a FAIL on a single **Minor** — scenario IDs in code doc-comments, fifteen occurrences across six files — which bites because of this project's own `spec-disposal: archive` setting, and which is the same defect ranked out of an otherwise clean PASS earlier in this MVP; closed across three commits **without deleting a sentence**, each comment now stating the substance the citation stood for and every surviving pointer naming something that outlives the archive. Pass 2 returned **zero findings at every severity on all three dimensions**, and its FAIL is one scenario verdict, `PARTIAL` on the closure — the reviewers were right and **the miscount was the conductor's**: the search that sized the class matched only `FR-\d+\.\d+-S\d+`, so two thirds of it were invisible to the instrument that declared it closed, which is the same failure the spec spent eight phases finding in fixtures, committed by the person policing it. No pass 3 was commissioned, and that is a ruling: the remaining item is *mechanically checkable*, so it gets the mechanical check. The sweep that followed produced the constraint that outlives it — **the rule is not "no document references in code", it is no reference to something that dies**, and a regex widened to catch a decision identifier would destroy exactly the durable citations of `standards/` and `CLAUDE.md` this repair asked for. Per-reviewer counts sum exactly to the merged counts in both passes, checked because an absent reviewer and a clean one look identical. **92 scenarios, gate exit 0 at the closure commit unpiped, 1 193 tests, coverage 94.36%**; one transient failure is recorded rather than smoothed over, and the failing test's name was lost because the conductor's own output filter could not match nextest's indented `FAIL [` line — an instrument that destroyed the evidence it existed to collect. Manual acceptance was performed in the **shipped binary** from a playground working directory with a **relative** root, the configuration that was inert before phase 7: a misspelled field printed the refusal naming file, block and field within seconds, and a second, different mistake printed a second, distinct refusal. **What that does not cover is stated plainly: an accepted reload prints nothing by design, so its player-visible effect has not been observed by a human** — and this spec's own history is why that sentence is there. Deferred: **PRO-944** (four stated pieces of scope — the composition-root move, the `Watching::{TheRoot, Nothing}` obligation, the relevance rule's deviation from the ruled shape with the vendor's absolute spellings still crossing the port and per-event filesystem I/O under a relative root, and the removed-`blocks/` asymmetry whose shipped behaviour today is **silence**, the worse of the two candidates), **PRO-948** (a player is shoved clear on join and on local launch too, refused as a fold-in because it needs its own scenario, RED and mutation record, and because its wording differs — the player was not there when the content changed — and it must be sited so local launch, resume and MVP 3's join all inherit it), **PRO-949** (the injectable-worker seam that would witness `WorkerGone`, the one load-dependent reload fixture, and the output filter), **PRO-950** (12 spec identifiers in 11 files outside this spec's manifest, five of them in production sources, deliberately untouched here because fixing them would widen the diff past the manifest the gate reading was taken against) · `feature/PRO-918-hot-reload`
- 2026-08-18-entry-door-clearing · 2026-08-18 · high · entry-clearing, admission-door, module-private-constructor, compiler-held-invariant, seating-scan, enumerated-verdict, needle-prefix-hole, compose-write-split, adaptation-window, mutation-criterion, documentation-guard, mc-sim, mc-client · **A player entering a world is never left inside solid rock, and it is one door that says so.** PRO-918 built the clearing search and wired it to a single caller — the content-reload swap — so a block declared `solid = true` while the game was off resumed a player inside rock, silently, with undoing the edit the only way out. Being placed somewhere unblocked becomes a property of **entering a world** rather than of reloading content: `mc_sim::simulation::seat(spawn, world, content) -> Seated` runs the search, moves the player and carries the verdict out, and **`Simulation::new` loses `pub` entirely — not `pub(crate)`** — so exactly one thing in the workspace can construct a `Simulation`, and *no player enters a world unchecked* is **compiler-held rather than scan-held**. The cheap alternative is what that buys: a call added to `simulation_at_launch` can be deleted with the whole suite green, and it would never have covered the generated-world door at all. **What the compiler does not hold is written down rather than implied** — a second seating path added *inside* `simulation.rs`, which MVP 3's join is exactly the shape of — so FR-1.3 reads the crate's own sources and answers a total enumerated verdict with three positive controls, and the residual hole is named exactly: a second `pub fn` that *calls* `seat` and discards the `Clearing` still runs the rule and loses only the reporting, which is tolerable because the thing that must not be skippable is the search rather than the sentence. **The price is a type change, and a call-site grep cannot size one** — 24 `Simulation::new` sites, ten `Result<Simulation, LaunchError>` positions in one support module that are not call sites at all, a `Launched` alias, and files needing an edit while naming none of it: 37 files and 82 needle sites against an inspection set of 43. Commit B leaves the tree uncompilable for one commit, and what makes that acceptable is a procedure rather than a hope — a throwaway `seat`/`Seated` stub applied to three sources, `cargo clippy --workspace --all-targets --all-features -- -D warnings` and the full suite run against the adapted fixtures, the stub reverted **by hand** and `git diff --exit-code` confirmed clean before explicit paths were staged. The zero-window route was refused on arithmetic: a return type cannot be migrated in place, so three public functions would stand in both shapes at once and the migration commit still touches every file — the window is traded for a larger diff and a temporarily doubled public surface, not for less work — and one argument for it is recorded as **not available**, since `main` receives one commit either way under a mandated squash merge. **The search is unconditional**, and the alternative was refused on coupling rather than on cost: `RegistryVerdict` is computed and dropped inside `load_world`, so gating means widening `mc-world`'s persistence return to let `mc-sim` ask persistence whether to ask the physics a question, and it buys the two block reads a clear player costs — `cleared` opens with an `overlaps_solid` early return and the 2 601-candidate ring is walked only by a player genuinely trapped. FR-1.2-S2 is what makes that falsifiable: a launch over a save whose blocks all still match their declarations (SPEC-020 retired the argument that phrase named; the falsifier is unchanged and is the save, not the command line). **The reload's own two sentences had been asserted by nothing at all for as long as they existed**, because `report_clearing` sat behind a `wgpu::Surface` and a `winit::Window` nothing in this workspace constructs — repeating that shape would have left FR-2's whole stakeholder promise ungraded — so composition moved into total functions of a `Copy` verdict needing no device, and **four sentences are now asserted where none were**. The *write* is a separate obligation with its own instrument: a subprocess cannot reach it (`shipped_binary.rs` works only because a missing content root refuses **before** a device is opened, and a successful launch opens one), and a unit test of the composer is agreement between two copies of one decision, so a source scan grades the wiring — deleting the single call line reddens 1 of 275 and nothing else in the workspace. **A substring needle matches its own extension, and it bit twice here**: `) -> Seated` is satisfied by `) -> SeatedPlayer {`, so a rename left the scan green over a door that no longer hands back the type the rule names, and `fn say_entering` was satisfied by `say_entering_now`; the first was found **only because a mutation was run after its row already said what it was expected to do**, which is the argument for running the ones whose outcome looks foregone. A bare `rg` is the wrong instrument — `production_text` drops every `///` and `//!` line, so the shell and the scan report different numbers — and the `published:` needle's prose exposure is one `///` → `//` edit wide and **open and accepted**, because the shape it is paid to catch is a struct-literal second door and a red naming `reload/mod.rs` is a minute's diagnosis. **Thirteen mutations plus M13b, and the misses are the part worth keeping.** Four of phase 1's six bit **wider** than predicted and share one cause: every entry scenario reads the player through `Simulation::latest()` alone, so any defect upstream of the first published snapshot reddens all of them together and "this scenario only" was unreachable by construction. M1's real value ran backwards — it turned the one baseline red **green**, which is what established that the fixture was failing because entry clearing moves the player it seats and not because of anything else in the tuple it asserts. Three did not bite and each establishes something different: **the velocity rule has one witness and it is not at entry** (1 of 1 209, through the reload caller, so a change dropping that path's clearing takes the rule's only witness with it); **the const idiom protects singleness, not unwrapping** — inlining both clauses and running `cargo fmt --all` left them unwrapped at 115 and 148 characters, since no `rustfmt.toml` exists and rustfmt never reflows a string literal, and what reddened instead was duplication; and **M13b has no instrument at all**, hand-wrapping one clause across a `\` continuation leaving 275 of 275 green with the idiom abandoned, reachable by a person and not by rustfmt, so **only review sees it**. Each direction of a unification is seen by a *different* scenario test and only an additional-coverage test sees both. **Two skeletons were not enough, and the third was measured rather than planned**: the over-eager one re-centres a player already horizontally cell-centred with a whole-number `y`, so the move is a no-op and the generated-spawn scenario would have entered the phase unfalsified — the one thing it exists to prevent. **Entry clearing broke a PRO-918 control and it was restored rather than weakened** — this project's only instrument for *the clearing search is never reached from the refusal path*. The admission door moves a trapped player before any reload happens, so the fixture asserted a position the run no longer produced while its premise guard went on passing because it measured the **declared** spawn rather than the **seated** one; the state is now reached the way a player reaches it — a pocket packed solid through the whole search so entry leaves them embedded, a head cell of water so their eye can aim out, one break straight up to open the position — and the guard takes the published snapshot, so handing it a declared constant is unspellable. **The documentation guard forced the producer to be extended rather than the recogniser dodged**: quoting the entry sentences as prose would have bought a green suite by making the page unguarded, which is the exact failure that harness exists to catch, so the harness now produces them through production's own composer. Its grammar is strict where prose is not — one fence per sentence carrying the real coordinates, never a fence holding two and never an `(x, y, z)` placeholder, because every line of a fence is joined into one string and compared whole. **Two assertions redden a correct implementation if written the obvious way**: the first-snapshot scenario is read at **tick 0**, since a cleared player arrives `on_ground: false` and tick 1 settles them by falling a fraction and landing, and `centre_of` has no `+ 0.5` on `y`; both cheapest greens — grounding the player, editing the search — are Out of Scope. **A file-size cap evicted an explanation, silently**: the rationale for saying the notice only after the uploads succeed was written as a comment in `app/mod.rs` and dropped to stay inside the 500-line cap, with nothing going red and no deletion for a reviewer to see, so `docs/technical/architecture.md` is its only home — and that file now measures exactly 500 lines with **zero margin**. **D8 stands deferred**: the walk-and-needle scaffolding was copied a fourth and fifth time rather than extracted into `mc-testkit` — seventeen test files now define their own `production_text` — because each guard is worth exactly as much as a reader's ability to see the whole of it in one file, and a shared engine makes every guard weakenable by an edit made for another guard's sake. Revisit at a sixth guard, or when the copies are found to have drifted in a way that mattered. Validation PASS: pass 1 returned FAIL on three **Minors** with zero Blockers and zero Majors, all 18 scenarios verdicted PASS across 39 recorded verdicts, three candidate findings all confirmed by adversarial verification and none filtered out — checked against the run journal, because a clean merged result and an absent reviewer look identical. **No pass 2, and that is a ruling**: every finding was cosmetic and mechanically checkable, and a mechanically checkable item gets a mechanical check rather than a second 700 000-token review pass. Two of the three were fourteen-space runs folded into five assertion literals by a `\` continuation written through a *Python* string — a Python continuation deletes the backslash and the newline and **keeps the following indentation**, so rustfmt never touched them — closed whitespace-only and provably so, with the house re-wrap written and then reverted because the extra source lines push two functions past `clippy::too_many_lines`: **a cosmetic improvement is not worth spending structure on**. The third was `INDEX.md` naming a count the page owns, closed by deleting the number rather than correcting it — **a routing summary describes what a page answers, never how many answers it has.** 18 scenarios, 14 tasks, three phases, 1 221 tests, 94.36% lines / 92.54% regions, gate exit 0 twice on a tree confirmed quiet before and after. **No human has played this** — the journey is step 6 of `docs/modding/hot-reload.md`: quit standing in water, set `solid = true` offline, relaunch and read the line (SPEC-020 retired that argument: the relaunch now needs nothing on its command line and prints two lines). Known and carried forward: M13b's absent instrument; the `published:` prose exposure; the velocity rule's single witness; **a join can still supply the wrong ground** — the omission it cannot make is forgetting the rule, and whichever spec adds the join owes it a scenario; `docs/user/gameplay.md` quoting both entry lines from outside the walked tree; `app/mod.rs` at zero margin; and PRO-953, a memory-cap refusal test that failed once under full-suite load and is deliberately **not** quarantined, because one observation is not intermittency and that test is the witness for *a bad mod never takes down the server* · `feature/PRO-948-entry-door-clearing`
- 2026-08-18-grass-block-art · 2026-08-21 · high · per-face-texture-keys, baked-art, voxforge-build, texture-manifest, texture-set-index, stable-fold, launch-verdict, mip-chain, linear-light-averaging, terrain-sampler, art-gate-stages, derived-not-committed, appearance-hash-revision, image-basis, plane-axes, dihedral-discrimination, orientation-claim, golden-re-shoot, scope-retirement, ADR-026, ADR-027, ADR-028, mc-core, mc-world, mc-render, mc-sim, mc-client, voxforge · **The world stops being teal, and how it nearly did not is the finding.** A block declares `texture` as one key for all six faces or as a table naming one per facing, `voxforge build` bakes a committed manifest of model-and-face entries into a named image set plus an index folded over exactly the sources it reached, and the client fills each assigned layer from that set's image where it covers the key and from the generated stand-in where it does not — per key and never per set, so a mod author's first undrawn block draws something and keeps running while a checkout that never ran the build is refused by name with the command to fix it. Terrain now magnifies with nearest and minifies with linear over a five-level CPU mip chain averaged in **linear light** (bytes 0 and 255 reduce to **188**, not 128 — every level of the wrong implementation is darker than the one above it, and a scenario saying "midway between" would have accepted both), anisotropy refused because wgpu prices it against crisp voxel magnification (ADR-027); the set is derived and never committed, held by two gate stages that build the art before the tests on both coverage paths and skip the test stage **recorded rather than silently** when the build refuses; and a save's appearance hash goes to revision 2 folding all six declared keys, so every pre-spec save reports its blocks as changed through the existing acceptance path — correctly, because every block's appearance really did change. PRO-902 closed at both of its sites and neither parses a block name any more. **Then the part worth the whole entry: the renderer drew five of its six faces turned, and 1366 passing tests were blind to it.** Every reading this feature built measured *which colours* a face holds and none measured *where they sit* — means, histograms, distinct-colour counts, pairwise ΔE, landmark shares and `share_within_any` are **all invariant under rotation, reflection and permutation**, and so is "the four sides are pairwise unequal"; the one scenario that judged a face judged the **top**, which was the single row the table had right, so it was green and honestly so, which is worse than a false green because it makes the covered case look representative. The goldens had been minted from the broken output and enshrined it. **It was found by the project owner looking at a golden frame.** **The fix was not where it was diagnosed, and that is the second finding.** `PLANE_AXES` is the geometry's extent→world mapping and exchanging its rows *moves the mesh* — tried, and measured: east and west stayed red at **1104 of 1104** while north and south went green, one table serving two masters. A pair of axis indices cannot express an image's basis at all, so the image basis became **two derived tables** (`IMAGE_SWAPS`, `IMAGE_SIGNS`) computed as `const fn` vector arithmetic over each facing's own outward normal, because **a table of conventions cannot be checked by reading it** while a normal carries no convention to get wrong (ADR-028, extending ADR-013). And `build/validate.rs` had text-compared **three hand-written copies of that constant** for five increments, all three in agreement **by machine** and all three wrong — **a mechanical check that two copies match closes drift between the copies and says nothing whatever about either.** What closes the hole: FR-8.1-S6 compares each of the six baked images to the model's own outermost plane **texel for texel**, with a discrimination proof derived from the image's own symmetries rather than written down (identity scores 0 on all six faces, every other dihedral transform 82–210 of 256, and 82–170 of 256 texels differ from each image's own horizontal mirror — top 170, east 82); S7 and S8 split a drawn face's vertical and horizontal halves, because **a pure lateral reversal preserves turf-above-dirt** and neither claim implies the other. **Both horizontal facings still have no discriminating test**: the bottom row was corrected on the strength of the bake because a block's underside is never seen here, the top/bottom convention is now *stated* on both sides (`voxforge` bakes the top image's top edge toward `-z`) where the baker's page used to leave it "to whoever consumes the texture" — an orientation contract neither side states is one neither side can be wrong about, so both were — and **the first anisotropic top or bottom texture owes a scenario.** Three further lessons, each measured: the mirror figure first read "224 of 256", which **no shipped image gives** — relayed from a report into a specification unmeasured and read past by everybody including its own author, caught only when a fixture author had to *compute* it, which is the **third figure of that shape** here and names the only reliable defence (a figure nothing computes is a figure nothing checks); **reading the tree found three of twelve** pinned premises that a correct T53 would redden and **running it found twelve**, because a fixture computing its expectation from `SHIPPED_KEYS.len()` is invisible to a search precisely by being derived rather than committed, so the remedy is to land the change and read the failures — and two of the twelve came back **stronger** than the premise they replaced; and an oracle that shares production's fallback **moves with the draw**, so killing `SuppliedTexels::covering` left `terrain_probes` green by agreement rather than by drawing, and only a mutation narrowed to the device path could answer the wiring question — which it did, finding **six witnesses besides the goldens**. **FR-8.2 was retired whole with all seven of its scenarios on the owner's ruling** — the three Python generators are one-off provenance, nothing re-runs them, so byte-reproduction is not a property this repository holds any more and the salt search inside them is deliberately unguarded and *said* to be; recorded rather than deleted, because a reviewer meeting a requirement that is simply gone cannot tell scope-**retired** from scope-**dropped**. **The single sentence that must not be lost to the prune**: a texture-key change marks **every** section and `take_remesh_work` drains the whole dirty set into **one** batch, and only those two facts together make the retained-but-not-re-packed state unreachable — so **bounding the re-mesh batch** (a whole-world re-mesh measures 9.1 ms and is exactly the thing somebody will one day bound) turns it into a production path, with every test green, unless the retained sections are re-packed against the serving resolution first. Validation **PASS** after two three-specialist passes and six findings, **every one documentation and not one a code or test defect**; five were statements this spec's own work falsified, and acting on the *class* rather than the two instances found **ten more** neither pass had reached — the worst being `modding/README.md`'s first-block tutorial, the first thing a mod author reads — so the habit is recorded in `docs/technical/working-in-this-repo.md`: a spec that *lifts* a limitation has no author in the passages that recorded it. Pass 2's scenario coverage is a **sample and says so**, with three honest abstentions, one of them unreachable because the review manifest had been narrowed from 187 files to 74 — an instrument that cannot see something reports zero rather than reporting that it cannot see, which is the same shape as the defect this spec was built around. 104 scenarios, 50 tasks, nine phases, 1370 tests, 93.55% lines / 92.05% regions, gate exit 0 on four readings — and **the commit that fixed the central defect was not itself gate-green**: its new doc comments linked from public items to private ones, which rustdoc refuses outright, so only the gate could see it. Carried forward: both horizontal image rows resting on the bake and on an ADR paragraph; `docs/planning/client-server-split.md`'s layer-index sentence and `crates/mc-sim/CLAUDE.md`'s TOML sentence, both falsified by *other* specs and left for their own commit on `main` because one spec is one commit; and **no human has played this increment** — the journey is `cargo run -p voxforge -- build content/base/textures.toml` then `cargo run -p mc-client`, and look at a grass block's side · `feature/PRO-947-grass-block-art`
- 2026-08-21-changed-save-reports · 2026-08-22 · low · save-acceptance-default, changed-blocks-notice, inverted-flag, laundering-on-quit, one-shot-report, policy-is-not-wiring, subprocess-witness, bounded-subprocess, device-cost, scenario-falsified-by-measurement, unreachable-refusal, targetability-fuse, limitation-sweep, synonym-sweep, prompt-that-never-existed, ADR-029, mc-world, mc-client, mc-sim · **A save whose blocks changed *behaviour* now loads and says so, instead of being refused.** Accepting is the default in `acceptance_from`, only a **missing** block still refuses, and the strict answer stays behind an inverted `--refuse-changed-blocks`: one line to the error stream naming every changed block, ascending and complete, said **above any device** so a run of the built binary can see it, and nothing at all when nothing changed. Water was declared `breakable = false` in the same breath, which is what made the mechanism fire on a real content edit rather than on a fixture for the first time. **The flag earns its keep on a property no test asserted and no document recorded** — `NameTable::record` rewrites a save's recorded hashes from the registry the process is running, so **load → play → quit destroys the mismatch forever**, the report is a one-shot, and somebody restoring a doubtful backup wants the world left shut because opening it is what destroys the evidence. It was owed *documentation* rather than a test, since nothing here created it, and it is now in ADR-029 and `world-format.md` §"Known limitations, as built". **"Policy is not wiring" was measured twice, on both paths, and each figure is the whole argument for spending a subprocess**: the binary ignoring its own `argv` reddens **1 test of 1386**, `prepare_launch` ceasing to call `say_changed_blocks` reddens **1 of 1385**. Every other reading of either sentence reaches the composer through a launch or calls it directly, which is agreement between two copies of one decision — the binary can stop consulting its own parse, or stop saying the line, and no library-level test in this workspace can tell. **The two readings differ in cost and the reason is where each sentence is said**: the notice is written from the preparation worker above any device, so a bounded read catches it with no GPU, while a refusal over the *save* surfaces only in `App::collect_preparation` inside a redraw — so the refusing reading **needs a window and cannot not**, and answers a **failing** verdict on a machine with none, because an absent instrument and a clean one must not look alike. **`Command::output()` is not safe here**: under the `argv` mutation the child loads the world and its window never closes, so it wedged for **606 seconds** and had to be killed by hand — both binary readings are bounded now, the same mutation answers in about a second and names the defect, and the price is that an ordinary `cargo nextest run` may **flash a window** with no environment variable able to suppress it, since `gpu_startup::open` passes `Backends::PRIMARY` as a `const` and `WGPU_BACKEND` is never consulted. **A scenario of this spec was falsified by measurement rather than argued about, and it is the finding worth the entry.** S12 said a player could no longer break water; `targeted` returns a hit only where `is_solid` answers true, water declares `solid = false`, so the ray walks **through** the water cell and `broken` is never called on one — measured before anything was implemented, a break aimed at water answered `Changed { from: Holds(base:stone), to: Empty }`. `Refusal::Indestructible` is **unreachable for the one shipped block that declares it**, the declaration is inert for a player, and what shipped instead is the break's real answer asserted by a test that **reddens the day water becomes targetable** — with the handover written on the variant and on `targeted`, where somebody changing targetability reads, rather than in a spec folder PRO-904 will never open. The player capability this spec actually delivers is the other one: a world that opens after a content update, with the changed blocks named. **The old default had been documented as a prompt that never existed** — it was always a refused launch naming a flag — and of 36 grep hits for prompt-or-ask language measured on 2026-08-21, five were coincidental and **31 lines in 14 files described this mechanism as prompting or asking** — that is what the 31 counts, and it is neither the number of lines the diff removed nor the number of files touched — five of them shipped `mc-world` doc comments arguing *from* the prompt. The worst was player-facing and false about the shipped program, `gameplay.md`'s "the game asks before opening a save whose blocks behave differently", **on a page corrected for this same class two commits earlier** — it survived because that sweep grepped the limitation's own phrase and this line used a **synonym** for it, which is now the third shape recorded in `working-in-this-repo.md` §"Lifting a limitation costs one grep per place it was stated": the grep is over the limitation's *vocabulary*, and a wrong account of the old behaviour is not made findable by lifting it. Two skeletons, because an empty-output skeleton passes an absence scenario for the wrong reason — inert gave **13 failed of 1385**, every one an assertion failure and not a compile error, and the over-eager one reddened the five absence readings the first could not. **Five mutations and all five bit**, including one that could not have been driven red by any skeleton (`water.luau` losing `replaceable = true`) and one whose only witness is a scenario nothing else reddens (`refuses` dropping its emptiness guard, 50 failed). `RegistryVerdict.retextured` kept its field and lost its promise: its doc comment claimed a caller that has never existed, so the sentence was **retracted** rather than the promise kept, and what replaces it is the reason the field is load-bearing — it is the third arm of a **total** classification, which is what lets a test compare the whole verdict instead of asserting an absence. Validation at `low` is the green gate and nothing heavier, by explicit project-owner ruling: 15 scenarios, 1386 tests, 93.63% lines / 92.09% regions, gate exit 0. Carried forward: `Refusal::Indestructible` inert until PRO-904 splits targetable out of solid, **which owes the scenario S12 could not be**; the laundering-on-quit property asserted by no test and held by documentation alone; the window an ordinary test run may flash; and **no human has played this increment** — the journey is to quit an existing world, edit a block's declared behaviour offline, relaunch with nothing on the command line and read the line · `feature/PRO-956-changed-save-reports`
- 2026-08-22-solid-split-properties · 2026-08-23 · high · blocks, meshing, targeting, save-format, golden-frames, hot-reload, mc-world, mc-sim, mc-client · **One bit answered four unrelated questions, so the one shipped block declaring itself non-solid was completely invisible.** `drawn`, `occludes` and `targetable` become three independent optional booleans, each defaulting to whatever the *same* declaration says about `solid` — the only non-constant default on the page, and what lets every declaration written before them go on meaning what it meant. `solid` keeps its name and narrows to collision alone; renaming it `collides` would have invalidated four shipped declarations, 42 test files carrying declaration text and a line-for-line doc check, to gain a clearer name on a field whose documentation was being rewritten anyway. **The player capability is the sea**: water declares itself drawn, non-occluding and targetable, so it shows its surface and edges with the lakebed through it, a swing at it is refused as `Indestructible` with the block behind untouched, and a placement still builds through it. That refusal variant had been **unreachable for the shipped content** and its fuse — planted by SPEC-020 in four sites with the handover written on the variant itself rather than in a spec folder — is blown here, which is what that handover was for. The mesher decides a face by **three questions over a shared key table** — `drawn(self) && !occludes(beyond) && key(self) != key(beyond)` — the third an **engine rule over key identity rather than a declaration**, so `visible_face`'s standing property that no name and no runtime id is read anywhere survives verbatim; `merges_with_self = false` has exactly one identified use and belongs to PRO-952, which bumps the appearance byte anyway, so deferring the field costs no extra bump. `Boundaries` becomes `[[Key; 256]; 6]` because a plane of booleans has no room for the third question and the shipped sea spans sections. **No shipped content had ever reached that third question** — occlusion culled first every time — measured by deleting the clause and watching `scene_contract`, `replay_world` and all four then-committed `r1` captures stay green, which means a future mutation removing it and moving the goldens is the rule *beginning to bind* rather than a defect. `SolidVoxels` becomes **`ResolvedVoxels`** carrying two bitsets read through two narrow traits, `Solidity` and `Targetable`, never one trait with two methods: the nine sites reading solidity mean collision by it, and a single trait would hand each of them a question it must never ask. ADR-015's principle held under exactly the pressure that would have broken it — the guarantee was never "one bitset" but a mirrored view kept in step through **one private write path**, and the second view went through the same two functions with `set` taking both answers in one call so a caller writing one without the other is unspellable. That is also what makes what a swing can find follow an **edit** rather than only a load, which nothing writing a cell and reading it back can distinguish. **Both save-format revisions move, in opposite directions and for opposite reasons**: `targetable` joins **behaviour** (`BEHAVIOUR_REVISION` 1→2) because it is what makes `breakable = false` reachable at all, `drawn` and `occludes` join **appearance** (`APPEARANCE_REVISION` 2→3) because a block that stopped being drawn is still the same block to stand on — routing a rendering field through the behaviour byte would tell every player in existence that every block they built with behaves differently. The two constants are what the arrangement is for, and **that both have now moved is not an argument for one**. A new field is **appended and never inserted**, because `postcard` encodes positionally: a rename costs nothing, a middle insertion moves every following byte and makes the revision byte report a change larger than the one that was made — which also removes save compatibility from the list of things the boolean defaults had to serve. **Only a test stating the byte sequence by hand can see a revision byte move**; four files do, and which of them a given move reddens depends on which list moved — leaving `BEHAVIOUR_REVISION` at 1 reddens exactly the two behaviour-stating guards at **1435 tests run: 1433 passed, 2 failed, 1 skipped**, `--no-fail-fast`, complete, while both appearance guards stay green and the whole-verdict guard over the committed pre-spec save stays green under it too. `SCENE_REVISION` bumps for the **first time**, `r1`→`r2`: not because water became drawn — the sea emitted faces **no declared capture could see**, quad count 2759→2770 with every `r1` golden still matching on a real device — but because the **declared spawn moved to the coast**, which is a product change made for a test's benefit and recorded as one, since a human could already walk to the sea and only the automated capture could not. `r2` was minted **twice**: the first ranking put an optional property above the water margin the requirement rests on, and re-ranking exposed the worse fault — **the candidate filter never required the spawn column to stand above sea level**, so the corrected ranking's top was submerged. A criterion can be wrong in a way re-sorting it cannot reveal; state the filter and the ranking separately. Raising `SEA_LEVEL` was rejected instead because moving the camera moves the goldens (which move once anyway) while moving the world moves the **scene contract**, and the first level putting water at the spawn's feet submerges 37% of the world. **Validation passed on pass 2 with zero findings across all three dimensions, 3/3 reviewers returning real payloads.** Pass 1 returned one coverage GAP, FR-2.6-S3, and it was **the scenario that was wrong**: a quad count is a property of the merge rather than of the world — the mesher emits the scanline-greedy decomposition and deliberately not the fewest rectangles, so a merger growing columns before rows reports a different number for identical geometry and any agreeing derivation is a copy of its subject. **The guard proposed for the residual was false and was measured false**: a packed terrain vertex carries no field derived from a quad's extent, texture coordinates come from a corner's own section-local position and the sampler repeats on all three axes, so a re-partition is **pixel-neutral by design** and the goldens could not witness merge shape if they tried. The exact-partition proptests are what witness it, and they are strictly stronger than any area sum because they catch a face relocated with its area preserved — though they witness the **partition and not the predicate**, since their generated pool declares only `solid` and the scan they compare against asks solidity on both sides, so a widened pool owes the scan the same three questions. Gate green at `95e06eb`: **1435 tests run: 1435 passed (6 slow), 1 skipped**, a bare count and a complete run, lines 93.63% regions 92.13% over 10 844 tracked. Thirteen documentation sites across four pages needed changing and **three were gate-visible**, two of those correct as they stood and needing defending rather than fixing — so a mechanical sweep is as dangerous as a missing one, and a doc sweep's provenance matters more than its result. Carried forward: **no human has played this increment** — walk to the shore, swing at the sea, read the refusal, build into it, then quit and relaunch and read the four-block line once; the judge's first-drawn-voxel march assumes every drawn block is opaque, with PRO-952 the named breaker there and for `merges_with_self`; and `ResolvedBlock` was deliberately **not** extended, because `ClientContent::is_solid` has no production caller and adding surface with no production reader is the policy-is-not-wiring trap — handed to PRO-944, which moves the composition root · `feature/PRO-904-solid-split-properties`
- 2026-08-24-medium-properties · 2026-08-25 · feature/high · medium, swimmable, move-resistance, save-revision, goldens · Water becomes a medium: `swimmable` and `move_resistance` on a block declaration, a player who swims, `BEHAVIOUR_REVISION` 3, water's `1.6` derived from a measured admissible window, goldens re-shot at `r3` · `feature/PRO-957-medium-properties` · 8 phases · 9h18m
- 2026-08-26-framerate-independent-speed · 2026-08-26 · fix/high · pacing, frame-clock, catch-up-bound, reload-report · A player warped at super speed: the world advanced one tick per drawn frame. A frame now spends whole tick quanta out of the elapsed time, capped at 250 ms. · feature/PRO-971-framerate-independent-speed · 4 phases · 3h08m
- 2026-08-26-absolute-pointer-fallback · 2026-08-26 · fix/high · rdp, pointer-regime, winit, absolute-pointer, calibration · Mouse look spun over RDP: winit's relative guard tests a zero flag, so screen positions were spent as deltas. Only a measured-absolute stream is differenced against an anchor. · feature/PRO-962-absolute-pointer-fallback · 4 phases · 3h07m
- 2026-08-26-water-texture · 2026-08-26 · fix/high · textures, uncovered-key, goldens, detection-gap · The sea drew the stand-in: water declared a texture key nothing baked. Art baked, and declared keys are now read against the built set — two lists no renderer produces. · feature/PRO-972-water-texture · 4 phases · 3h12m
- 2026-08-27-swim-ascent-rate · 2026-08-27 · feature/high · medium, swim-ascent, declared-rates, behaviour-revision-4, goldens · Water felt like a swamp. A third medium field, `swim_ascent`, lets content declare the rise; the sea sinks in two seconds and swims at two thirds of a walk. · feature/PRO-992-swim-ascent-rate · 8 phases · 8h27m
- 2026-08-27-water-translucency · 2026-08-28 · feature/high · opacity, blended-pass, translucency, packed-vertex, r5-goldens, ADR-030, ADR-031 · A block declares a degree of opacity and terrain draws a second unsorted blended pass, depth-write off. The sea shows the lakebed; a mod may declare glass. · feature/PRO-993-water-translucency · 9 phases · 12h39m
