-- The all-opaque fixture's dirt. See `stone.luau` for what this root is for and
-- for why `drawn` is stated rather than defaulted.
return {
	name = "base:dirt",
	texture = "base:dirt",
	solid = true,
	drawn = true,
}
