This directory carries no `default.provenance.json`, which is the third fault
the scan in `tests/golden_inventory.rs` has to report and tell apart from the
other two.

The file you are reading exists only because git does not track an empty
directory. Nothing reads it.
