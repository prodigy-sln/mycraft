# Documentation Index

Registry and routing guide for the living documentation. `docs/` describes
the system **as built** — never planned or future behavior. Future concepts
live in active specs and `product/roadmap.md`.

**One deliberate exemption:** `docs/planning/` holds design discussions for
features still in the planning state (see `planning/README.md`). Nothing in it
describes built behaviour, nothing in it is binding, and a spec that lands
supersedes it. It exists so that deep pre-spec analysis has a home in the tree
instead of living only in an issue tracker.

**Issue-tracker references, by audience.** A page written for a **mod author
or a player** names no issue. That reader has no way to open one, so the
reference means nothing to them the day it is written and dangles the day the
issue closes — and unlike a missing reference, a dangling one is not visibly
missing. State the substance instead: what is true today, what an author
should do about it, and that something better is coming.

A page written for an **engine reader** may name one, **provided the reference
stays supplementary to substance that stands without it.** That reader is a
contributor with the tracker open, and stripping the references would delete
the only findable record of several real gaps. But the clause is the whole
rule: *"see PRO-000"* rots into meaninglessness the day the issue closes,
while *"X is true because Y, tracked as PRO-000"* still tells the reader
everything they need after the link is dead. A tracker reference is never the
substance.

`docs/planning/` is exempt from both, per the paragraph above — naming the
issues a design elaborates is most of what those documents are for.

Consolidation updates this file: add the source identifier to the Sources
column of every updated file, and register newly created files.

## Structure

```
docs/
├── INDEX.md              ← you are here
├── technical/            ← engine contributors: architecture, protocol, formats
├── modding/              ← content authors: start at `modding/README.md`; per-kind contracts beside it
├── user/                 ← players — currently just movement and controls; server operators still planned
├── planning/             ← pre-spec design discussions; exempt from the as-built rule (see planning/README.md)
└── ops/        (planned) ← running a public server: deploy, backup, moderation
```

Branches marked *planned* have no directory yet. They are created by the spec that first produces
documentable behaviour for them, not in advance.

`modding/` is the project's most externally-visible contract. Because the base game is itself a
mod, anything in `crates/mc-script`'s public binding surface belongs here — and a change that
breaks it is a breaking change for every server in existence.

## File Registry

Only files that exist are listed. A file appears here when it is written, not when it is planned —
the Routing Guide below is where future destinations are declared.

### technical/

| File | Purpose | Sources |
|------|---------|---------|
| architecture.md | Crate boundaries and dependency direction: the registry/loader seam, the simulation/renderer snapshot seam, the player's intent/physics/collision model, the client input dispatch (`Session`, the drivable core, and what stays unreachable), the editable world (break/place, the store/collision mirror, the raycast reach bound, the refusal vocabulary and the one variant the shipped content cannot reach, with the handover written on it, the three-armed reads that keep "no such cell" and "empty cell" apart, and the whole-world mesh that reads without marking anything dirty), the remesh transport that makes an edit visible, saving and loading a world (the `mc_world::persistence` module and its encoder confinement, how the save path reaches the blocks through `mc-sim`, the two preparation entry points and which one the resume decision sits inside, and `mc-client`'s wiring-only role), the HUD across three crates (the `HudElementSource` port, its own fault type, the format-agnostic raw declaration, the single composition entry point with the zero-HUD path preserved beside it, and the debug overlay's clock port, non-scriptability and reopening condition), the pure/GPU feature seam inside `mc-render`, the reporting seam that turns a client-ending failure into printed text (one renderer, one sink, and the three constructor doors that close composition outside `mc-render`, why it sits beside `exit_code` rather than in `mc-client` for the same coverage-visibility reason, why a way-out sentence is appended after a chain rather than woven into it, and the boundary sentence tabulating every non-fatal notice that still writes to stderr directly, outside any sink), and the invariants asserted mechanically rather than by convention (including the one-way `tools/ → crates/` dependency walk), and the scripting host's seams (the backend confined to one adapter directory, what is Rust-side and keyed by the attachment versus what lives behind that adapter, the load-bearing construction order, the one latch over two limits, the never-re-entrant dispatch queue drained across rounds, and why the client reaches the host deliberately now that block declarations are Luau — with the retired closure guard's reasoning and what replaces it), and the Luau block loader with the seam it sits behind (evaluation guarded as an entry into script, the raw key enumeration and the metamethod it actually meets, the four content-root bounds and why their order is fixed, the chunk name against the origin, the simulation loading content while the client receives it resolved, the four chokepoints watched in the client's own sources, what crosses that seam and what deliberately does not, and the layer assignment stated rather than derived — with the measurement showing the wiring is behaviourally unfalsifiable here and the scan is the only instrument that can see it), and hot reload's seam from a saved file to a swapped registry (the watcher behind a port with its one-file litmus test, which saves count and why that rule is derived from the loaders' own constants, the settling window declared once, why the no-wall-clock rule survives a debouncer, the candidate built off the tick thread through the existing content door, the all-or-nothing swap through the world's second write door and why that door is a child module, the content published through a second `ArcSwap` with its serial, and the three client-side properties held by shape — a batch carrying its own registry, a discarded batch's hand-back inside the collect, and a retired layer spent but not live — and where a reload puts a player it trapped: the cell-centre candidates, the sideways-first order over a cube that has no downward offsets, and the eligibility rule that makes outside the loaded world unknown rather than clear, with the three cheaper shapes it refuses, the helper whose name overstates what it answers, and what a future change must not break), and the seating door every entry into a world passes through (why it is the simulation's constructor rather than the launch path, what a module-private constructor holds against every caller and the one hole it cannot hold — a second `pub fn` that calls the door and discards the verdict — how MVP 3's join inherits the rule rather than the function and which mistake it can still make invisibly, when the factoring that leaves that mistake possible should be revisited, why the search runs at every entry rather than only where the load reported changed blocks — and why that stays forbidden now that the load does report them, which makes the cheap implementation spellable where it was not — where the verdict is parked and why "said once" is structural rather than a dedup field, why the clearing notice is said only after the uploads succeed while the changed-blocks notice is said above any device, and what each of those two placements buys — an explanation a file-size cap evicted from the code, silently — why a cleared player arrives not yet on the ground and settles on tick 1, and why the move is self-limiting through the save), and the texture-set index the art build writes and the client reads (why the format lives in `mc-core`, the three load-bearing properties of its line shape, the fold's stated byte sequence and its length prefixes, the manifest-relative recorded paths, `voxforge build` in the topology with the three shape decisions behind it, `TEXTURE_EDGE` enforced from the build side as well as the renderer's, and the client's launch verdict on a built set — a total enumeration returned in `Ok` with the two arms that let a launch through, the re-fold of the recorded sources rather than a second read of the manifest, the fixed order of the questions, the hard-coded output directory and the dead end it leaves, the error arm the plan could not have listed, and the field `thiserror` would not let be called `source`), and the decode at the composition root — the one client file that may name an image decoder, the renderer that holds no filesystem type, the two launch doors that each read a set once, and the supply given to a renderer at construction and deliberately absent from the reload path), and the two narrow views one resolve settles together (why collision and what a swing can find are two traits over one type rather than one trait with two methods, the single write path that keeps all three views in step and what taking both answers in one call makes unspellable, why what a swing can find has to follow an edit rather than only a load, the raycast asking targetability with its reach bound still a single site, the placement that lands one step back except into a `replaceable` cell and why it has to, and the refusal variant that became reachable the day aiming stopped being derived from collision), and how a frame paces the simulation now that the two are no longer one cadence (the single clock reading given to both the overlay and the pacing and why taking it twice is the thing that is unspellable, whole quanta spent by subtraction rather than by a division that floors three quanta to two, the 250 ms catch-up bound derived from a 10 fps floor with its surplus discarded, the private per-tick step that makes the old shape not compile, the measured residual hole where deleting the wiring leaves the whole suite green and the three non-assertions standing in its place, why the end-to-end test cannot exist while the surface takes `Fifo`, and the table of every per-tick effect a frame reads once with the Blocker that table found), and the pointer regime (`winit`'s relative raw-input guard testing a flag whose value is zero, so a Remote Desktop pointer's screen positions reach the camera as device counts; the position-shaped predicate and why one component over 1000 suffices where requiring both would have failed on its own recording; the state table and the three properties it buys; the two routes that forget the anchor, why the focus route can never be reached by a sample, and the turn the missing one left standing; the per-axis calibration written down as an assumed nominal with its measured error; and the two rejected alternatives, `primary_monitor()` and a plausibility clamp, each rejected on the data rather than on testability) | SPEC-002, SPEC-004, SPEC-005, SPEC-006, SPEC-007, SPEC-008, SPEC-009, SPEC-010, SPEC-012, SPEC-013, SPEC-014, SPEC-015, SPEC-016, SPEC-017, SPEC-018, SPEC-019, SPEC-020, SPEC-021, SPEC-022, SPEC-023, SPEC-024, SPEC-030 |
| decisions.md | Architecture decision records — stack choices and their rejected alternatives, including where a reported failure's rendering seam lives and why its construction is closed to three doors rather than left to a scan alone, array-texture layers appended within a session and never renumbered, a reload candidate that stops declaring a block the world holds being refused whole, and whether a generated asset is committed following from whether it can be reproduced rather than from its being generated — with golden frames named as evidence rather than assets, and the generator run as an explicit pre-build step rather than from a build script — with that record now implemented and its five items placed; and terrain magnifying with nearest and minifying with linear over a full mip chain, anisotropy refused because the vendor prices it against crisp voxel magnification; and a save whose blocks changed *behaviour* being loaded and reported rather than refused — only a missing block refuses, the line is said above any device so a run of the built binary can see it, and the report is a one-shot because a quit rewrites the hashes it was about; and the amendment recording that a mirrored view kept in step through one private write path survived a second view being added — the count changed and the guarantee did not | PLAN.md, SPEC-002, SPEC-004, SPEC-005, SPEC-007, SPEC-009, SPEC-010, SPEC-013, SPEC-014, SPEC-015, SPEC-017, SPEC-019, SPEC-020, SPEC-021 |
| licensing.md | The shipped `LICENSE-MIT` and `LICENSE-APACHE`: their hashes and byte counts, the provenance chain a replacement must re-establish (and the ecosystem variant that would fail a check), why the Apache appendix stays unfilled and why that makes a shared placeholder detector report a byte-perfect file, where the copyright holder is written and that nothing derives from it, how much SPDX the drift check understands and every construct it refuses by name, the three vacuity guards, where the check lives and the three constraints that put it there, and what deliberately does not exist (`NOTICE`, `license-file`, per-crate texts, general link validation) | SPEC-011 |
| rendering.md | The section mesher's quad, determinism and error contracts (including the empty cell answered before the registry); the terrain draw path as built (packing, array texture, compute culling, single indirect draw, depth, pass configuration); texture layers stated by whoever read the content and honoured here rather than derived, over a key set no world can move, and what a block whose declared texture is not its own name does at packing time; the HUD pass (the pinned rectangle derivation, the outline ring composed before fills, alpha compositing in linear space, the borrowed array texture, the rectangle ceiling, and the derived prediction whose duplication is the oracle); the draw-target conventions (orientation, capture pixel format); the procedure for re-shooting a committed golden set; what golden-frame and probe verification cannot see, measured against the player's own camera and against the HUD's per-pixel prediction; and the excavated-world capacity gap against the scene's quad budget; layers appended within a session and never renumbered, what renumbering would do to vertices already in the renderer's hands, the 256-layer bound as the packed vertex's own width, a retired layer spent but not reclaimed and the relaunch that reclaims it; and why a reload that changes what is drawn re-meshes the whole world — with the measurement refusing the selective rule and the reason a drawn observable cannot see the difference; and the mip chain as arithmetic — why levels are averaged in linear light rather than over stored bytes, the byte that separates the three implementations, why the chain is built on the CPU instead of in the subtree coverage does not count, the surface as built with its two refusals, and every level of it uploaded; the terrain sampler as a requested value rather than a private descriptor — nearest magnification, linear minification, linear interpolation between levels, anisotropy refused by the device rather than pre-checked, and the two pairs of readings that keep a request and its consequence apart; and the re-shoots on record, with why `SCENE_REVISION` was not bumped for an art change, why one phase took no bisect reference capture, and the bump the scene contract could not ask for — a camera path moved by content-declared physics over an unchanged world, which the tripwire cannot see and for which there is no guard; and which faces exist stated as three questions — is this block drawn, does what is beyond it fail to occlude, are they different blocks — with the third an engine rule over key identity rather than a declaration, its named breaker, the boundary plane carrying a key rather than a flag with the four properties a change must not break, and the measurement showing no shipped content reached the third question until the sea was drawn; the reload's geometry key as drawnness and occlusion rather than solidity, and why editing `solid` alone still redraws through the loader's default rather than through the key; and why the goldens are not the merge-shape witness, a re-partition being pixel-neutral by design; and the spec that gave the sea baked art — why `SCENE_REVISION` held at `r3` while four golden directories were re-shot in place (layers are assigned positionally over the keys the declarations name and `base:water` already sorted last, so baking changed what sits *at* a layer and not the index riding inside every packed vertex), and the set comparison that proved the sea's extent did not move rather than merely counting that it had not | SPEC-001, SPEC-003, SPEC-004, SPEC-005, SPEC-007, SPEC-008, SPEC-010, SPEC-012, SPEC-016, SPEC-017, SPEC-019, SPEC-021, SPEC-022, SPEC-025, SPEC-030 |
| testing.md | Quality gate stages (why its result may never be read through a pipe, why a zero exit code is not sufficient, how its two file-length measures disagree, why a phase whose tree does not compile at its start has no gate evidence at its start, and the size stage's per-root file count across `crates/` and `tools/`), verification harnesses, derived probes and the scene contract, the derived-oracle pattern repeated at fixture and world scale (including a tileability verdict judged against a document's own declaration and against itself, never against a second render), why an independent oracle is still an unchecked one — the constitution's rule keeps the subject out of the expectation and says nothing about the oracle being wrong — and the three things an offline derivation owes beyond its numbers, the headless client-input harness and its mutation-count discipline, the break/place mutation table and its fixture-geometry and refusal-by-name lessons, why a requirement that nothing changes yields only guards, why a falsifier list must be measured rather than derived from call paths, why a diff-derived review manifest omits the riskiest files, the honesty conditions behind the 10 000-block exit criterion, persistence's second-entry-point rule and the `sync_all()` flush no test can catch, the three rules — a texture key's field, a section lookup's key, and a launch that generates nothing it does not need — that a reader holds because no fixture can, why nothing in the workspace runs `App` and why coverage cannot say so, why an instrument must be confirmed before a product is reported dead, why a test asserting the correct value can still be a defect and why a mutation claim must say how much it replaced, and what automation cannot check (including the manual acceptance checks no harness can drive, the closed-set properties nothing grades, and the human sign-off on VoxForge's preview loop — reliable for proportion and orientation, not for structure), the hostile-mod harness (its six named cases, the containment evidence each declares, the three-way verdict, and the text guard that stops it stating any policy of its own), where a missing mechanism wedges instead of failing and the exact-name overrides that bound it, the scripting host's mutation tables and the skeleton pairs beside them, the three lints denied at a crate root and the test target that denial does not reach, what the sandbox verification does not establish, why a printing path needs both a source scan and a subprocess run and what each alone misses (a report composed but never reached, invisible to the scan; a report reached but never rendered in full, invisible to the subprocess), and the documentation-drift guard that recognises a quoted refusal by its own printed shape rather than by an author-maintained marker (with the page-follows-run dependency that makes quoting an unproduced refusal a failure, and the parser-bump warning narrowed to the half a parser can still reach), why a bare `cargo nextest run` now needs the art built first and why the obvious repair deletes the refusal it is about, why an ordering fixture on this platform must falsify collation order rather than creation order, why a retirement's surface is measured by building rather than by searching for the type, why a phase's verification instrument is run where its answer is still attributable, why green-on-arrival and inert are separated only by a mutation, why a mutation that misses has two causes with different owners — a mutation too broad is narrowed by the implementer, a fixture that cannot see the property is the test author's to change, how a guard naming one dependency narrows as the guarded set grows, why a bare-spelling needle admits no exemption, why an accidental safety property has no guard of its own, the literal as the tell that you should go and look, why a bound derived from another bound bounds nothing, why the mechanical edit is where test-file ownership looks most like ceremony, why an absence is closed by a variant rather than a second accessor, and when a slow fixture is slow because the question is about size, how a reload is driven with no filesystem and no window and which of its four stages is drivable at all, why a fixture's patience is denominated in the settling window rather than in boundary crossings, why the reload latency target is a benchmark rather than a gate stage and why every timing discriminator here is wrong in the convenient direction, the one manual acceptance check no harness can drive, the one question those findings are all instances of — ask what the shipped caller supplies and which shipped path reaches this, because a fixture supplying an argument in a form no caller uses and a product path no caller takes are the two shapes every defect that survived six phases of green tests had — and eight things that seam cost in general terms (including that an instruction to update a figure is itself a claim measured before it is obeyed, and that a two-part task is reported per item rather than in aggregate), how a fixture offering a decision as a call of its own becomes the scenario that makes it, the two further `App` measurements and the one that had to be closed by the compiler, why an absence guard over a seam owes a presence guard over the wiring beside it, why a fixture supplying an argument in a form no caller uses tests a contract nobody has, why a two-part task is reported and accepted per item rather than in aggregate, and why a documentation guard keyed on the program's own prefix cannot tell a refusal from an ordinary notice — so a notice belongs in prose rather than in a fence until the guard's own run produces it, which is what the two entry notices did, and the entry door's own verification (real saves read through the shipped launch rather than in-memory simulations, why the exact-destination and exact-spawn assertions must not be loosened, why a composition behind a device nothing constructs is a composition nothing asserts, two source scans with enumerated verdicts and why one of them needs a control the other does not, why their reading orders are inverted and what masking that buys, why a bare `rg` counts the wrong thing with the standing counter-examples in this tree, why a needle may not be a whole sentence and why a substring needle matches its own extension, and both mutation tables including the outcomes that did not bite — the velocity rule with one witness that is not at entry, the const idiom that protects singleness rather than unwrapping, and the hand-written line continuation only review can see — and why guards of this shape are copied rather than shared behind one engine, with the condition for revisiting that), and three instruments this project has learned to distrust — that a figure nothing computes is a figure nothing checks, with the instances on record of a number that read as a measurement and never was, and the mechanism that keeps catching them; that a convention written in one file's header does not reach the next file, and that a mutation is the only routine occasion on which this project reads its own failure output; and that an oracle sharing production's fallback moves with the draw, so the wiring question needs a mutation narrowed to the device path alone; and that grading a binary through the process boundary has a cost the suite now carries in the open — two readings spawn the real client, one of them must reach a window, and an ordinary run may flash one with no environment variable able to suppress it; the four instruments that guard the mesh and their four different reaches — area by an independent per-voxel walk, merge shape by the exact-partition properties, what the camera sees by the goldens, and the quad count as a labelled tripwire — with why the goldens are *not* the merge-shape witness (measured: a re-partition emits the same texels), why a merge change has only two cases and neither is unreported, and the reach limit that those properties witness the partition rather than the predicate, plus what a widened generated pool would owe; how a gate's verdict is read from its log and the two things that look like verdicts and are not; and what a documentation guard cannot hold — measured at thirteen sites across four pages of which three were gate-visible, two of those correct as they stood, so a mechanical sweep is as dangerous as a missing one; and why a sweep asking whether a consumer is correctly paced never asks what writes what it reads — with the enumerate-and-classify rule a cadence change owes, the two properties that let that defect survive, and why a fixture's own patience window must exceed the bound it waits on or a slow machine reports the defect the fixture exists to deny; and a seventh manual acceptance check — the only one on the page run *over* Remote Desktop, why it does not license running the capture checks there, and the third way a tolerance goes wrong: derived from both directions, documented, and bounding the instrument's resolution rather than the subject's error, told apart by setting the threshold to zero and reading a flat zero back; and an oracle that is right and useless — a colour oracle answering `covering(key)` *or else* the same stand-in the product falls back to, goldens minted from the renderer they grade, and a gate comparing a set to the manifest that produced it: a closed loop with no outside reference anywhere in it, the three readings that break it, why a gate stage was the wrong remedy, and the tell, which is a discrimination guard with a *comfortable* margin | PLAN.md, sdd-init-project, SPEC-001, SPEC-002, SPEC-003, SPEC-004, SPEC-005, SPEC-006, SPEC-007, SPEC-008, SPEC-009, SPEC-010, SPEC-011, SPEC-012, SPEC-013, SPEC-014, SPEC-015, SPEC-016, SPEC-017, SPEC-018, SPEC-019, SPEC-020, SPEC-021, SPEC-022, SPEC-023, SPEC-024, SPEC-025, SPEC-030 |
| working-in-this-repo.md | **How this tree is actually worked**, as built — not rules about evidence and not standards: that a gate reading is a statement about a tree rather than a commit, and that a red you observed first-hand ages exactly as fast as one somebody reported to you; that a hot-reload acceptance run belongs in its own working directory so the collision is removed rather than coordinated around; that several agents share one working tree and one git index, so an unexpected failure is a question about who else is working before it is a question about what is broken (four measured instances, including a hand-run acceptance check mistaken for a content regression, and why the `index.lock` message recommends the destructive move); why `git add <paths>` is not explicit-path staging when the index is shared and what `git commit -- <paths>` does instead; how a file's own header is where its responsibility seam is found when a size limit forces a split; why a `pub` item on a `pub` type is invisible to dead-code lints and coverage alike, and where a load-bearing visibility gets recorded; and the three blind spots an edit loop has — a green suite says nothing about a lint, a green clippy says nothing about rustdoc's module-header scope, and a markdown-only change is not gate-neutral because the secrets stage scans the working tree and a long high-entropy literal in a documentation file is exactly what it is built to notice; and that a spec which *lifts* a stated limitation owes a grep for the limitation's own words rather than a re-read of the pages it touched, because the new surface documents itself while every passage recording the old limitation goes silently false — with the three shapes the stale copies hide in — an ADR whose Consequences name another document as their record, a tutorial example or routing-table digest restating the limitation in its own words, and a synonym that survived a sweep grepping the limitation's own wording, which is why the grep is over the vocabulary rather than the phrasing and why a wrong account of the old behaviour is not made findable by lifting it; that a split becomes `foo/mod.rs` because both layouts give the identical module tree and consistency is then the only live consideration, with the two sibling pairs left in the tree and why neither is a choice; and that a living page carries the command while a dated observation carries the number — with the one kind of document that cannot change as the standing exception, and a routing summary describing what a page answers rather than how many answers it has | SPEC-017, SPEC-019, SPEC-020, SPEC-022 |
| world-format.md | What a cell holds — a block or nothing — and the named two-variant type that keeps that apart from "there is no such cell"; chunk section and column storage; the block palette (including its bound under repeated edits); a section's storable identity as the persistence layer inherits it; the world's `WorldPos`-addressed column footprint; block-identity stability across a registry change; and the on-disk save format — the hand-read preamble, the two-value encoding, atomic replacement, the save-table identifier's width, the allocation-bounding security properties, the three-outcome load decision, and the determinism of a save's stored world data; a section answering solidity and drawnness as two separate questions and why they are two functions; and both folds' current field lists and revision bytes with which list each declared property joined and why, that a new field is appended and never inserted because the encoding is positional, and what the behaviour byte's first move costs a save that predates it | SPEC-002, SPEC-003, SPEC-007, SPEC-008, SPEC-009, SPEC-019, SPEC-020, SPEC-021, SPEC-022, SPEC-030 |

### modding/

| File | Purpose | Sources |
|------|---------|---------|
| README.md | **The way into `docs/modding/`.** What a mod is here and that the client reads one content root today; the four kinds of file and which two reach a running game at all; a complete first block **declared in Luau** — write one file, run one command, see it on screen and place it, with a guard holding the quoted declaration to actually loading; the held-block rule that makes a new block reachable and why it is a placeholder for an inventory; all-or-nothing loading, the five refusals, where each one stops the launch, and the full refusal a mod author now reads on their terminal — file, declaration and field named, read outermost first — with a real quoted example a guard checks against a live run; where each per-kind contract is written; and where the scripting host fits, which is the sandbox a block declaration itself runs inside; and the walkthrough's last step, which is to leave the client running and edit the file just written | SPEC-002, SPEC-005, SPEC-007, SPEC-010, SPEC-013, SPEC-014, SPEC-015, SPEC-016, SPEC-017, SPEC-019, SPEC-021, SPEC-022, SPEC-030 |
| blocks-items.md | Block authoring contract, **in Luau**: file layout and which entries are declarations, a declaration as a chunk that returns a table, all twelve fields with type, default and bound, the namespaced-id rule, all-or-nothing loading, how to read every refusal with a quoted guard-checked example, the four content-root bounds and their check order, why there is no empty block to declare, a complete two-block worked example, the two forms `texture` may take — one key for all six faces or a table naming a key against each of `up`, `down`, `north`, `south`, `east` and `west`, with the axis each word maps to, the shipped grass declaration as the worked example now that a base block uses the table form, and every refusal such a table raises — and what a key resolves to in pixels: the built set's image where it covers the key, the generated stand-in where it does not, per key and never per set, with declarations now reloading while the game runs and the contract for that on its own page, and which eight fields a save folds into a block's recorded behaviour so an author knows which edits every existing world will name on the terminal and which are silent — with water's `breakable = false` as the worked example of a declaration that now refuses a real swing; and being seen stated apart from being solid — `drawn`, `occludes` and `targetable` as three independent booleans each defaulting to whatever the same declaration said about `solid`, why that is the only non-constant default on the page and what a silently ignored one would cost, the wrong-kind refusal quoted from a real run, a complete worked example that runs (a pane you can see, see through and walk into), what consumes each of the three, why `targetable` is what makes `breakable` mean anything, `replaceable` deciding *where* a placement lands and why it has to, and the two different things that produce a changed-blocks line — a declaration you edited names what you touched, an engine build that grew the fold names every block once | SPEC-002, SPEC-007, SPEC-008, SPEC-015, SPEC-016, SPEC-017, SPEC-019, SPEC-020, SPEC-021, SPEC-022, SPEC-030 |
| hot-reload.md | **Hot-reload authoring contract:** the write-save-see loop; exactly which saves trigger a reload and which are passed over, and the 150 ms settling window; that the whole root is read every time, blocks and HUD together, accepted or refused as one; all-or-nothing swapping at a tick boundary; what survives (the world cell for cell, position, velocity, the tick, the save) and the one thing that does not; the held-block rule re-derived so a newly declared block arrives in an author's hand, and why that is a placeholder; which edits are visible — every declared field now among them, `texture` included, per facing — and that a re-pointed facing draws baked art where the built set covers the key and a generated stand-in where it does not; that a reload draws only from the art the launch read, so art baked mid-session waits for a restart; `breaks_into` naming nothing being accepted and failing at the break; the 256-layer session budget, appended-never-renumbered, spent by keys ever seen, and that relaunching reclaims it; what being moved clear of a newly solid cell costs a player, and what an author's own `solid` change does to whoever is playing — including that only ground the world actually holds counts as clear, so a player near the world's edge is moved inward rather than off the map and meets the same "nothing within eight blocks is clear" line when the ground that is left is solid too; every refusal an author can meet with the words it prints; and a complete worked example that runs, ending in the refusal that stops an author sawing off the branch they are standing on; and the same solidity edit made while the game is **not** running, answered at the next entry by the same search with its own two lines quoted, why those two are fenced and the reload's two are prose, that neither is a refusal and the launch always proceeds, how it sits beside the changed-blocks notice a load now writes and what `--refuse-changed-blocks` is for, and a further worked-example step that quits, edits offline, relaunches and reads both lines; and which of the twelve fields re-mesh and which change behaviour only — `drawn`, `occludes` and the six keys re-mesh, `solid`, `targetable`, `replaceable`, `breakable`, `breaks_into`, `swimmable`, `move_resistance` and `swim_ascent` do not — with why editing `solid` alone still redraws until you write the other two out explicitly, that a `targetable`-only reload is accepted and rebuilds nothing, and that the three medium fields take effect on the next tick without a section being rebuilt, so a liquid can be retuned while you stand in it and a refusable `move_resistance` or `swim_ascent` refuses the reload whole; and that a verdict reaches you whatever your frame rate, with the build in which it did not and why the once-said unwatchable-root refusal was the one lost outright | SPEC-017, SPEC-018, SPEC-019, SPEC-020, SPEC-021, SPEC-022, SPEC-023, SPEC-030 |
| hud.md | HUD authoring contract: file layout, every field with its requirement and mutual-exclusion rules, the nine anchors and the safe-area inset, UI units against the 720-pixel reference height, eight-digit colours and linear-space alpha, the outline convention and its two-pass ordering, the published draw kinds and readable values, why a root declaring no HUD is valid where one declaring no blocks is not, what content categorically cannot do, and why the debug overlay is unreachable from content | SPEC-010 |
| script-writing.md | *How do I write one?* The scripting set's entry point, and the page that states once what is authored in Luau today — blocks are, behaviour is not. The shape of a chunk and the attachment it is registered under; the returned value field by field, including every malformed shape passed over in silence; the two authoring rules that pull against each other (a cursor in a closure upvalue, and build-then-swap because an abort cannot be cleaned up in script); and one complete chunk exercising all of it, with what it costs, what it retains and what it categorically cannot reach | SPEC-014, SPEC-016 |
| script-surface.md | *What may it reach?* The exact reachable and denied global sets, why the four interesting denials are denials, why `require` is absent rather than confined, why `print` is an escape and not a logging question, and the frozen per-chunk environment that makes a global assignment an error, and the explicit statement that player input reaches no scripted surface at all — no hook, no callback, no value for keys, mouse buttons, cursor capture or pointer motion of any kind, the client's own choice of pointer regime included, and no declarable binding table | SPEC-014, SPEC-016, SPEC-024 |
| script-limits.md | *What may it spend?* Every shipped limit with its default, what exceeding it does and what an author does about it; the two-memory-figure constraint that makes a misconfigured host refuse to start; why the budget counts calls and loop edges rather than instructions; and the measured tick arithmetic worked forwards to price a workload and backwards to size a slice | SPEC-014, SPEC-016 |
| script-faults.md | *Why did it break?* What a fault carries, the rendering grammar and the four origin shapes, each kind with the exact text it produces and the script that causes it (including why `error(text, 0)` is what gets a clean cause, and why an allocation refusal's two figures are a delta and an absolute), quarantine as an author meets it, and why a fault raised under host memory pressure names nobody | SPEC-014, SPEC-016 |
| voxel-models.md | VoxForge (`tools/voxforge`) — the `.mcvox` authoring contract: file layout, the implicit-single-part and explicit `[[parts]]` document forms, the per-model/per-part `slice` orientation table, palette and namespaced material files, part attachment and states, the 64-per-axis size bound and (0,0,0)-normalised assembly, `inspect`'s stats/defect/observation partition and why connectivity is not a structural-soundness check, `preview`'s fourteen canonical views and contact sheet, `texture`'s flat-shaded faces and three-leg seam verdict, `build`'s manifest-to-named-set bake (where the files live, every manifest and entry field with its type and bound, the derived image file name and why a key does not become a path, the whole-set delivery and the index it writes, the fold as cache key and what a presence-only image check means, the advisory unused-key scan and its computed-key limitation, every refusal quoted from a real run, and a complete worked example from four files to a named set), what the client makes of a built set at launch (the six verdicts and which four stop it, the re-fold of the recorded sources, the four refusals quoted from a real run with how to clear each, the four ways a set is not readable at all — an index recording a source outside the root, an image name that is a path, an image no layer can be filled from and a file that is not a PNG, the last two naming the key as well as the file — the `output = "art"` dead end, and why an uncovered key is never a refusal), and the refusal contract; what a stand-in looks like on sight, that a flat two-tone checkerboard and not any particular colour is the tell, and that nothing but your own eye will report one; and the shipped models' provenance — which generator produced which model, the one that writes the file the repository tracks and why that is still not licence to re-run it, and water's palette with the part of it approved rather than derived | SPEC-013, SPEC-019, SPEC-025 |

### user/

| File | Purpose | Sources |
|------|---------|---------|
| gameplay.md | Player-facing controls and movement feel: WASD walking, mouse look, jump, cursor capture, the two mouse buttons that break and place; what is on screen (crosshair, held-block swatch) and the F3 debug overlay; saving on quit and resuming on relaunch — drawn as saved from the first frame, not only walkable as saved — what a player sees and can do when a save can't be loaded outright, that a broken content file now stops the game with a terminal message naming the file, the declaration and the part at fault instead of a single generic line, and that every block a player breaks and places is now declared in a Luau script while nothing about playing changed — same blocks, same terrain, same saves — with behaviour still not scriptable and the sandbox refusing a mod file that loops or eats memory; and that content edited while you play now reaches the world without a restart — what survives it, that a change either all happens or none of it does, being moved clear of a cell that became solid and what that costs, that you are never put outside the world for it, and what a re-pointed texture comes back as — the baked picture where one exists and a generated stand-in where it does not, with art baked mid-session waiting for a restart; and that coming back to a save never leaves you inside a block — what you read on the terminal when a block became solid while the game was off, what happens when nothing within eight blocks is clear and why the game starts anyway, and that a fresh world moves you not at all; and that the world is now grass over dirt over stone drawn from real baked art rather than flat placeholder colour — what a player sees on a block's four sides, up close and at a distance, what a block whose art nobody has drawn does instead, and that the game checks the built art at every launch and will not start without it, so it is two commands rather than one from source; and that a save from before that build opens with nothing said about it even though every block in it looks different; and that a save whose blocks *behave* differently now opens too, naming them on the terminal in one line rather than refusing the launch, with `--refuse-changed-blocks` for somebody who would rather be turned away and why playing on destroys the evidence, and what water being declared unbreakable does and does not change; and that you can now see the sea — its surface and edges only, with the lakebed showing through, a new world starting you on the shore facing it — that you can aim at water and a swing at it is refused with what is behind untouched, so you can no longer dig through water to what is under it, that building into water is unchanged, and that a save from before this build names all four blocks on the terminal once rather than one; and that you can now swim in the sea rather than dropping through it — sinking gently instead of like a stone, holding jump to rise to the surface and float there, letting go to sink back to the lakebed, and moving at a little over a third of your walking speed while you do — with the save record having now moved a third time, so "told about once" is stated as once *per* change rather than once ever and a player who already met that line at the last build meets it again here; and that your speed no longer depends on your monitor — what the build that warped you around at 144 Hz was doing, that a struggling machine is jerkier rather than in slow motion, and that a long freeze is not replayed when you come back because at most a quarter second of it is caught up; and that mouse look works over Remote Desktop with nothing to switch on — what the old build's snapping and spinning was, that it may feel about 15-20% faster and why that depends on the size of the remote display, that there is no sensitivity setting yet and saying how it felt is the only thing that changes that, and that swapping between local and remote is handled while the game runs; what water looks like now that it is blue, what it looked like for the three days it was a magenta check and that this was not your installation, and that the terminal's stand-in notice never names the block using one | SPEC-005, SPEC-007, SPEC-008, SPEC-009, SPEC-010, SPEC-012, SPEC-014, SPEC-015, SPEC-016, SPEC-017, SPEC-018, SPEC-019, SPEC-020, SPEC-021, SPEC-022, SPEC-023, SPEC-024, SPEC-025, SPEC-030 |

### ops/

Not yet written. This branch is created by the spec that produces the behaviour it documents — see
the Routing Guide for where each topic lands.

## Routing Guide

When consolidating source material, update the files mapped to its topics:

| Source material about... | Update |
|--------------------------|--------|
| Architecture decisions, stack choices, rejected alternatives | `technical/decisions.md` |
| Crate boundaries, threading, tick model | `technical/architecture.md` |
| Chunk storage, palette, persistence, block-ID stability | `technical/world-format.md` |
| Packets, replication, interest management, QUIC usage | `technical/protocol.md` |
| Meshing, draw path, lighting, texture handling | `technical/rendering.md` |
| Test harnesses, golden frames, load rigs, benchmarks | `technical/testing.md` |
| What a mod is, where content roots live, how an author gets a first thing working, what reaches a running game | `modding/README.md` |
| Any `mycraft.*` Luau binding | `modding/api-reference.md` + the relevant topic file |
| How a chunk is shaped, what it returns, the authoring rules it is written under | `modding/script-writing.md` |
| Which globals a script can reach, what was removed and why, the frozen environment | `modding/script-surface.md` |
| What a client may evaluate, what content crosses the simulation/client seam, the layer assignment | `technical/architecture.md` (the seam) + `technical/rendering.md` (what honours it) |
| Sandbox limits, call-and-loop budgets, memory caps, sizing a workload | `modding/script-limits.md` + `technical/architecture.md` |
| Fault kinds and their rendered text, quarantine, fault attribution under memory pressure | `modding/script-faults.md` + `technical/architecture.md` |
| Hot-reload semantics and state migration | `modding/hot-reload.md` + `technical/architecture.md` (the seam) + `technical/rendering.md` (layers and marking) |
| How this repository is worked: shared tree and index, staging, where a file's seam is found, what lints and coverage cannot see | `technical/working-in-this-repo.md` |
| NPC authoring | `modding/npcs.md` |
| Quests, dialogue, storyline authoring | `modding/quests.md` |
| Blocks, items, tools, crafting authoring | `modding/blocks-items.md` |
| Voxel model authoring (`.mcvox`), VoxForge's preview/inspect/texture/build CLI, the texture manifest and the set it bakes | `modding/voxel-models.md` |
| HUD element declarations, screen-space composition, readable values | `modding/hud.md` + `technical/rendering.md` (the pass) + `technical/architecture.md` (the seams) |
| Player-facing behaviour and controls | `user/gameplay.md` |
| Server install, config, tuning | `ops/deployment.md` |
| Auth, identity, accounts, permissions | `ops/administration.md` (operator view) + `technical/protocol.md` (mechanism) |
| Anti-abuse, rollback, incident response | `ops/moderation.md` |
| Asset generation tooling, provider usage, cost control, whether an asset is committed | `technical/asset-pipeline.md` (see ADR-009 as narrowed by ADR-026) |
| Quality gate stages, thresholds, verification harnesses | `technical/testing.md` |
| Licence texts, the SPDX declaration, copyright holder, licence-file drift checks | `technical/licensing.md` (+ `technical/testing.md` for what grading an unalterable document costs) |
